# Changelog — Simatrix (repository root)

All notable changes at the Simatrix project root (spanning Module1, Module2, and the
topic deploy copies). Per-module changelogs live inside each module folder.

## 2026-07-25
- Fixed: Topics 5 & 6's Compare split could get stuck as a small floating "picture-in-picture" card (title bar + expand/close buttons) instead of the docked 50/50 split — a one-way narrow-viewport listener demoted the split below 768px but never restored it on widening. Removed the compact floating card entirely; Compare now has exactly one shape (the docked split) at every viewport width, restacking to a single column below 768px instead of switching UI (ADR-080). The identical listener + floating card still exists in `graphics_module_1_topic_3_points`, `graphics_module_2_topic_2_simple_positions`, `graphics_module_3_topic_2_development_of_surfaces`, and `Module2` — flagged as a follow-up backport, out of scope for this fix.
- Fixed: applied the same ADR-080 fix to `graphics_module_1_topic_3_points`, `graphics_module_2_topic_2_simple_positions`, `graphics_module_3_topic_2_development_of_surfaces`, and `Module2` — same byte-for-byte one-way listener + floating compact card confirmed in each, same removal (single docked shape, CSS-only restack below 768px). `template_starter`'s dead compact-card CSS/markup was cleaned too so new topics stop inheriting it. ADR-080 and RULES.md §5.16a widened from "topics 5+6 only" to platform-wide; the picture-in-picture Compare bug is now resolved everywhere it existed.
- Fixed: Topics 5 & 6's 3D True-Length BIS dimension (extension/tick marks + filled arrowheads) read correctly only in the Top quick-view — Front/Side showed a skewed parallelogram with edge-on arrowheads, because the standoff direction was a fixed world-up vector, screen-perpendicular to the rod only from directly overhead. `dimensions.js` gained a camera-aware variant (`addOrientedDimension`/`orientDimension`) that re-rolls the dimension's standoff to face whichever camera is live every render frame — Top is an exact fixed point of the new formula, so it cannot regress (ADR-081).

## 2026-07-24
- Added: every shipped topic (10) + `template_starter` now posts `{ type: 'sim:ready' }` to `window.parent` once boot fully succeeds and webfonts are painted, so the host site's loading screen can close on genuine readiness instead of guessing (ADR-078, narrows ADR-002's postMessage ban) — see `RULES.md` §2.10, `PLATFORM-RULES.md` §1.10, `ARCHITECTURE.md` §6.

## 2026-07-23
- Added: Topics 5 & 6's 2D Compare drawing gained drag-to-pan + scroll-wheel zoom (cursor-anchored, clamped 0.4–5×, double-click reset) — Module 2/Points' ADR-054/055 interaction, re-expressed against the topics' own ADR-076 ortho camera instead of a Canvas2D `project()` lens, since that lens doesn't exist there (ADR-077).

## 2026-07-21
- Changed: Topics 5 & 6's 2D Compare sheet moved off its shared-canvas scissor pass onto its own `WebGLRenderer`/canvas, so the two topics could finally adopt Module 2's ADR-037 floating-card workbench (grey shell, real gutters between the three cards, `#rail-toggle` Hide/Show) — a single scissored canvas couldn't show a gutter between its own two halves (ADR-076).
- Fixed: Topics 5 & 6's construction launchers (Traces, True Length & Angles, Rotation Method) forced the Compare card down to the small floating PIP instead of the 50/50 split; they now re-parent into the workbench rail like any other driver and run inside the expanded split.
- Removed: the two topics' `computeRegions()`/`pass()` scissor-region machinery (ADR-074's pixelRatio conversion fix along with it) — superseded, no longer applicable now that each renderer owns its own full canvas.

## 2026-07-20
- Changed: Topics 5 & 6's 2D Compare sheet scale moved from a fixed 150 mm span (ADR-038/ADR-072) to the Module 2 intrinsic-size model — it now derives from the line's own True Length, so a typical drawing fills the sheet instead of floating tiny in a worst-case-sized frame (ADR-075).
- Fixed: 2D-sheet labels desynced from the WebGL drawing in the Compare workbench on HiDPI/scaled displays in Topics 5 and 6 (devicePixelRatio was applied twice in the scissored passes). (ADR-074; RULES.md §3.33)
- Changed: platform-wide `--color-vp-line` promoted `#bc5d1e → #b25718` (Topic 6's local AA-driven override, ~4.92:1 on paper) across all 11 definition sites — Module 1 topics 2–5, Module 2 + its two topics, Module 3 topics 1–2, `Module1/src/shell.css`, `template_starter/` — plus `DESIGN.md` (front-matter, §2.1 table, §6.1) and `Module1/CLAUDE.md`; consumers reading `var(--color-vp-line)` needed no touch.
- Changed: Module 1 Topics 5 & 6 `body.compare-split` workbench (`#compare-card` + `#workbench-rail`) gained `border-radius: var(--radius-md)` and the split grid gained `gap: var(--space-1)` so the rounding reads clear of the flush panes.
- Fixed: Topic 5's stale CSS comment citing a nonexistent `src/labelLayer.js` corrected to the real `src/labels.js`.
- Verified: a stabilization audit's claim that Topic 6's `src/labels/` directory was unimported dead code was wrong — `lineRig.js` imports `createLabelManager`/`DIMENSION_OFFSET` from it live; deletion was skipped, the directory is untouched, and a real consolidation with the flat `src/labels.js` (two parallel CSS2D label systems in one topic) is banked as a follow-up task.
- Changed: step-card `.card__lead`/`.step-body` typography normalized to Module 2's `--text-sm` / `--color-ink-secondary` scale platform-wide — Topic 5 had drifted to a visibly larger `1.125rem`/`1rem` size, and Topics 3, 5, 6 rendered their step-body prose near-black instead of grey (ADR-073); `DESIGN.md` §3.2 amended to match, retiring the now-unused `--text-lead` role.

## 2026-07-19
- Added: `graphics_module_1_topic_6_projection_of_straight_lines/` — promoted the teammate-contributed Lines problem-solver from its untracked, non-conforming `module_1_topic_lines/` folder to the next free Module-1 catalog slot; it and the sibling `graphics_module_1_topic_5_projection_of_line_types` concept primer are distinct-by-design topics (six-position primer vs. 5-step problem-solving build), not competing versions, so neither was ported into the other (ADR-072).
- Fixed: `graphics_module_1_topic_6_projection_of_straight_lines/CLAUDE.md` described its Problem Library as deferred though `main.js` already wires `initProblemLibrary(...)` into `window.simAPI` — corrected the stale doc to match the shipped, active 12-problem library (verbatim N.D. Bhatt / K.C. John set, RULES.md §6.7).
- Fixed: `graphics_module_1_topic_6_projection_of_straight_lines/index.html` construction-aid tokens (`--construct`/`--locus`/`--tl-green`) were aliased to unrelated neutrals and missing their `*-ink` variants; reconciled to DESIGN.md §2.2's platform-catalogued hex.
- Changed: `../DECISIONS.md` ADR-038 gained a 2026-07-19 amendment recording the two standalone Lines topics' deliberate `SHEET2D_SPAN = 150` (vs. the original literal `300`) as a sanctioned deviation, not undocumented drift; new ADR-072 records the topic-6 promotion itself.

## 2026-07-18
- Added: `graphics_module_3_topic_1_sections_of_solids/` — 3 conic-section problems completing the KTU set (circle, isosceles triangle, hyperbola on a cone), joining the existing ellipse/parabola pair; the triangle/hyperbola pair share `angleDeg:90` and are told apart by a checked `offset` (0 vs 8 mm), a deliberate reversal of every prior conic problem's "offset stays free" default since at 90° the offset is a 1:1 dockable quantity (ADR-071).
- Changed: Consolidated the Module 3 doc set into the root single source of truth — elevated all 13 topic-local ADRs (`ADR-M3T1-001..006`, `ADR-M3T2-001..007`) into root `DECISIONS.md` as `ADR-058..070`, deleted both `graphics_module_3_topic_*/DECISIONS.md` files, repointed every citation (docs and shipped source comments) to the new numbers, added both topics to `ARCHITECTURE.md` §2 as BUILT, and added `RULES.md` §6.21/§6.22 mandating the `EXCLUDED_TYPES` hard-filter pattern for syllabus problem-kind exclusions.
- Added: `graphics_module_3_topic_2_development_of_surfaces/` Phase 4 (final) — textbook problem library with 4 verbatim shortest-path problems (pentagonal-pyramid string, hexagonal-prism ant, two cone round/half-round classics), the KTU 2024 "exclude through holes" rule enforced as a hard `EXCLUDED_TYPES` type-axis filter (ADR-069, completing ADR-065), a tolerant never-auto-fill self-check, and the shortest path drawn as a straight chord across the unrolled 2D pattern AND wrapped onto the 3D solid (per-face isometry lift, plum `Line2`, new `--color-dev-path` token) — revealed only on a matched self-check via a non-rebuild overlay commit (ADR-070); terminal-step "Complete & next problem" CTA + toast; verified live end-to-end (P1 chord 109.6 mm vs 109.7 closed-form, cone geodesic peak 0.548 u exact, stale-matrix render bug caught+fixed, 50 rapid rebuilds clean).
- Added: `graphics_module_3_topic_2_development_of_surfaces/` Phase 3 — truncated developments end-to-end: parameter dock wired (`src/uiManager.js` rewritten from the dead scaffold copy; solid picker + Angle-to-HP/Cut-height cutting-plane sliders, plane always ⊥ VP per topic ADR-068, docking into the workbench rail), topic-1's `sectionCut.js` clipper copied in so the 3D solid truly truncates (crimson cap + plane quad), and the 2D sheet draws the truncated pattern via `computeCutDistances()` fed a seated-frame plane derived by matrix inverse + `d − n_y·h/2` centring shift (state outside ShapeData, ADR-067); fixed the engine's corner sampling to the meshes' real `polygonVertexAngle` alignment phase (plain `2πk/N` read prisms/pyramids at corners they don't have). Verified headless: 30° cone cut matches the closed form to 6 decimals with zero NaNs, prism/pyramid patterns correct, memory flat across 50 rapid cuts.
- Added: `graphics_module_3_topic_2_development_of_surfaces/` Phase 2 — 2D flat patterns on the Compare sheet: new pure leaf `src/developmentEngine.js` (Parallel-Line stretch-outs for prisms/cylinder, Radial-Line sectors/fans for cone `θ = 360°·(r/L)` and pyramids `φ = 2·asin(s/(2·L_e))` — the KTU method split, topic ADR-066), the Compare card + 50/50 workbench ported from the Module 2 master with pan/zoom, ADR-053 intrinsic-frame sheet scale, and a `computeCutDistances()` truncation seam awaiting the topic-1 section-UI port.
- Fixed: MODULE-STARTER.md §3.3 no longer claims `meshAnalyzer.js`/`vertexLabeler.js` ship inside `template_starter/` (they never did — the sanitised template holds only the six platform leaves; both arrive via the `Module2/src/` copy), ending the contradiction with the file's own Case-C sanitisation note.
- Added: Scaffolded `graphics_module_3_topic_2_development_of_surfaces/` — the second Module 3 topic, duplicated from `template_starter/` per MODULE-STARTER Case A, with the Module 2 solid-geometry engine restored byte-identical (the eight files plus `meshAnalyzer.js`; `iShape.js` taken verbatim from the master per topic ADR-064, the conscious ADR-027 resolution) and `main.js` booting/resetting a default cylinder through the `rebuild()` seam. The syllabus "exclude through holes" mandate is enforced structurally from day one (no CSG, closed single-shell generators only, problem-`type` exclusion axis reserved — topic ADR-065); the 2D stretch-out/unroll layer is deliberately net-new for a later phase.

## 2026-07-17
- Added: `graphics_module_3_topic_1_sections_of_solids/` Phase 4 problem library — 4 verbatim textbook problems (square-pyramid section, cylinder ellipse, cone ellipse Fig 14-20, cone parabola Fig 14-28) with scaffolded hints, a section-aware self-check (±0.5° plane-inclination tolerance; live `'generator'`-angle target so the parabola cut checks against the real cone, ADR-063), a cuts-the-solid guard, the KTU 2024 "exclude true shape given" rule enforced as a hard `EXCLUDED_TYPES` filter (ADR-062), and a Step-5 "Complete & next problem" CTA with success toast; verified live end-to-end (parabola green at slider 69° and typed 69.44°, guard fires on a missing cut).
- Added: `graphics_module_3_topic_1_sections_of_solids/` Phase 3 drawing layer — first-angle top/front/side views from a byte-identical Module2 `projectionDrawer.js` copy (topic ADR-060, zero-drift), 45° apparent-shape hatching per view, and a TRUE SHAPE auxiliary sheet drawn 1:1 from the cut loop's plane coordinates with a "Face the section" camera tween (ADR-061 — no auxiliary camera, so no auto-zoom drift); stepper grown to 5 stage-gated steps; verified: 30° cone cut yields an exact ellipse (conic residual ~1e-8), top view foreshortened by exactly cos 30°, memory flat across 50 re-cuts.
- Added: `graphics_module_3_topic_1_sections_of_solids/` section-cut engine — hand-authored single-plane clipper (`src/sectionCut.js`, topic ADR-058, no CSG library) with welded loop chaining, crimson True-Shape cap (`--color-section-face`, DESIGN.md §7.3), live slider-driven re-cutting through `rebuild()`, restored `meshAnalyzer.js`, and Step-1/2/3 dock wiring; key robustness rule discovered: the clipper's on-plane snap must exceed the 1e-3 weld lattice or grazing cuts weld non-manifold.
- Added: Scaffolded `graphics_module_3_topic_1_sections_of_solids/` — the first Module 3 topic, duplicated from `template_starter/` per MODULE-STARTER Case A, with the eight-file Module 2 solid-geometry engine restored byte-identical (`cube`, `cone`, `cylinder`, `genericPrism`, `genericPyramid`, `genericSolid`, `shapeData`, `iShape`.js) and `main.js`'s `createSolidMesh()` dispatch wired into the `rebuild()` seam. Section-cut geometry itself is deliberately not built yet — `meshAnalyzer.js` has no arbitrary-plane/Boolean support today, so that is flagged as net-new work behind its own ADR.

## 2026-07-13
- Fixed: `.wizard-toggle` / `.vp-cluster` / `.vp-controls` top clearance raised from a rigid `44px` to `calc(44px + var(--space-5))` (68px) across `Module1/src/shell.css` + 7 index.html copies (Module2, template_starter, foundations, spatial_framework, points, simple_positions, the orthographic-views topic) so the in-viewport controls no longer collide with the host LMS chrome when its accessibility text-scaler enlarges the header.
- Fixed: `.wizard-toggle` top offset raised `var(--space-3)` → `44px` across all 8 CSS definitions (Module1/src/shell.css + 7 index.html copies: Module2, template_starter, all four Module-1 topic clones, simple_positions) so the top-right wizard chevron clears the host UI overlay, mirroring `.vp-cluster`'s existing 44px top-left clearance.
- Fixed: the orthographic topic's Front-view silhouette no longer draws a false seam across the central body (base and body share the same depth, so their front faces are flush) — `castProjectors()` and `drawCompare()` now split the base-top edge into two shelf segments outside the body instead of one full-width line.
- Fixed: the orthographic topic's Side view now draws the real base-shelf edge (the base sticks out laterally past the narrower body, forming a physical corner) that was previously missing from both the 3D pane and the 2D Compare sheet.
- Added: the orthographic topic's three views now draw the through-bore and the two mounting holes — visible (solid) where a hole's axis is normal to that view's plane (a true circle) and hidden (dashed) where its axis runs in-plane — in both `castProjectors()` (nested dashed `LineSegments2` children so they inherit the visible view's fold/gating/disposal) and `drawCompare()` (new `strokeHidden()` helper).
- Changed: the orthographic topic's pane views are now exact dimension-constructed 2D drawings (ADR-050) — `castProjectors()` builds each view from `BEARING_BLOCK_DIMS` instead of filtering an `EdgesGeometry` silhouette, so false seams/open outlines are impossible by construction; the dashed bounding-box projector rays were purged (planes show only the drawing outlines) and Step-2+ camera framing tightened (margin 1.18 → 1.08).
- Fixed: the orthographic topic's Side view (PP) is now a closed, textbook-correct outline — dropped the FALSE base/body junction seam (real only in the wider Front view, so kept there) and injected the dome apex tangent that `EdgesGeometry` culls below the 20° silhouette threshold; verified functionally (Side view 398→299 segments: 100 false-seam removed, 1 apex added).
- Removed: the dashed bounding-box "envelope" clutter around the 3D Bearing Block; the dashed projector rays already show the cast extent.
- Changed: the orthographic topic's default camera moved closer (`(-12,8,12)`→`(-9,6,9)`, same Top-Left-Front pose, RULES.md §5.20) and Step 1 hugs the lone block tighter (dedicated tight framing margin) so the solid fills the viewport before the planes reveal.
- Fixed: the orthographic topic's PP fold now hinges DOWN onto the HP (Module 2 `PP_FOLD_TARGET` parity, ADR-049), landing the Side view beside the Top view in both the 3D unfold and the 2D Compare sheet — the old sideways fold taught a different layout than the platform's reference implementation.
- Changed: ADR-046 amended — bounding-box simplification now applies only to the dashed projector rays; the 2D view outlines (3D panes and 2D sheet alike) trace the Bearing Block's true silhouette via `EdgesGeometry`.
- Changed: the topic's reference planes are physically exploded apart with visible gaps and carry CSS2D "HP"/"VP"/"PP" pills; the wireframe Observer is now a flat CSS2D eye icon (ADR-049) — CSS2D DOM nodes verified disposed across rebuilds (RULES.md §3.5).
- Changed: renamed `graphics_module_1_topic_4_glass_box` → `graphics_module_1_topic_4_understanding_orthographic_views` (product renamed "Glass Box Visualizer" → "Understanding Orthographic Views", ADR-048) and swept the new path through ARCHITECTURE.md, this CHANGELOG, and DECISIONS.md.
- Added: RULES.md §5.20 — default 3D perspective camera poses must look from the Top-Left-Front, so the orthographic layout reads left-to-right (ADR-048).
- Fixed: the renamed topic's Reset button was silently dead (its only wiring lived in a file `main.js` never imported); it now correctly clears the lesson.
- Changed: the orthographic topic now enforces Module 2's Two-Weight Rule (`LINE_WIDTH_PX`) — visible edges draw at 2.5px and hidden (dashed bore/mounting-hole) edges at 1.5px, in both the 3D pane views (`castProjectors()`, new `VIEW_VISIBLE_PX`/`VIEW_HIDDEN_PX`) and the 2D Compare sheet (`drawCompare()`), so hidden lines visually recede instead of competing with visible outlines.

## 2026-07-12
- Fixed: `template_starter/main.js` `rebuild()` disposal now DEEP-`traverse()`s every child of `shapeGroup` (new shared `disposeObj` helper) instead of a shallow one-level loop, so nested `THREE.Group` domain geometry no longer leaks the WebGL context — mirrors the Glass Box fix and inoculates every future starter-derived topic; `node --check` clean.
- Added: DECISIONS.md ADR-042 (deep-`traverse()` disposal is mandatory; shallow child loops leak nested groups — discovered in the Glass Box build, verified flat 16→16 geometries across 50 rebuilds) and ADR-043 (Glass Box reference planes use functional hue-tinted glass — HP teal / VP amber / PP violet at 0.09 opacity — not paper-tinted, so they stay visible on the white viewport while upholding the Two-Cue Rule).
- Changed: RULES.md §3.3 now requires the deep `.traverse()` disposal and forbids the shallow child loop (cites ADR-042); ARCHITECTURE.md §2 registers `graphics_module_1_topic_4_understanding_orthographic_views` (then "Glass Box Visualizer", renamed ADR-048) with its completed 3D domain build and the new `src/glassBox.js` leaf.
- Added: Phase 4 scaffold — `graphics_module_1_topic_4_understanding_orthographic_views` (then `..._glass_box`) duplicated from `template_starter/` (template left untouched). `meta.json` set to "Glass Box Visualizer" / lowercase `beginner` with matching `index.html` `<title>` (ADR-026); `CLAUDE.md` re-pointed at the root EG docs (it is an EG-family topic, not a new discipline); `main.js` boots the clean empty scene straight into the 50/50 Compare workbench (desktop) with a placeholder 2D sheet, preserving the ADR-012 no-transform ancestor invariant. Domain content (Glass-Box geometry, `drawCompare()`, rail drivers) still pending.

## 2026-07-11
- Changed: `.gitignore` now excludes `*.zip` archives and the Repomix codebase-dump `.txt`, so full-project archives and generated dumps stay out of git history.
- Changed: Step-6 "Show dimensions" is now a Show/Hide **toggle** (not a one-shot) in Module 2 and the `simple_positions` clone — button swaps label + tracks `aria-pressed`, `stepper.js` click handler flips `state.dimensions` and drives `simController.setDimensions(on|off)` (the engine's `setDimensionsVisible` already handled off), so a learner can hide the dimensions again; done-badge lights only while on.
- Fixed: closed the `simple_positions` clone parity gap — its `main.js` gained the whole dimension consumer it was missing (`showDimensionsFlag`, `applyDimensionVisibility`, `setDimensionsVisible`, `refreshProjections` now parents `hpDimensionGroup`→`shapeGroup` + `vpDimensionGroup`→`vpFoldGroup` per the ADR-041 fold-split, `simController.setDimensions`, reset clears the flag) plus the Step-6 button + done-badge in `index.html` and the toggle wiring in `stepper.js`, so dimensions finally SHOW and fold correctly in the clone instead of sitting orphaned.
- Changed: DECISIONS.md ADR-041 amended to make the `hpDimensionGroup`/`vpDimensionGroup` split **mandatory for fold-survival** (HP dims → world `shapeGroup`, VP dims → `vpFoldGroup`) and record the Show/Hide toggle + clone parity; ARCHITECTURE.md §3 `projectionDrawer.js` note references the split-group fold necessity.
- Fixed: Module 2 front-view dimensions no longer stand upright after the flatten — `projectionDrawer.js` `dimensionGroup` split into `hpDimensionGroup` (top-view depth + distance-from-VP) and `vpDimensionGroup` (front-view height/width + clearance-above-HP), each with its own line batch, filled-arrowhead soup, and CSS2D labels; `main.js` `refreshProjections` now parents the HP group under `shapeGroup` (world, stays flat) but the VP group under `vpFoldGroup` so it folds down flat WITH the VP grid + front view, and `applyDimensionVisibility` / `setDimensionsVisible` drive both groups together. Backported byte-identical to the `simple_positions` clone's `projectionDrawer.js`. Headless-CDP verified (ADR-019): filled arrowheads render (HP 4 / VP 6 triangles), Step-6 reveal works, and at fold=90° the VP-dimensions Y-span collapses 4.02→0 (coincident with the folded VP), HP dims untouched, zero console errors.
- Changed: overturned Module 2's OPEN 3:1 chevron dimension arrowheads for FILLED 3:1 triangles (ADR-041 amended) — `projectionDrawer.js` now routes each arrowhead into a separate `arrowPos` buffer and packs it into one `MeshBasicMaterial({side:DoubleSide})` triangle soup added to `dimensionGroup` (dispose() broadened to free any material), so the Three.js layer matches the exam-grade filled BIS wedge the Canvas2D sheet + Bearing Block already use.
- Added: Step-6 "Show dimensions" reveal — `Module2/index.html` button + `#done-dimensions` badge, wired through `stepper.js` (`state.dimensions`) → `simController.setDimensions` → `main.js` `setDimensionsVisible`, which parents `activeProjection.dimensionGroup` under `shapeGroup` in `refreshProjections`, gates it on `showDimensionsFlag`, draws it on with a fade, and toggles the CSS2D labels per-object (r160 `CSS2DRenderer` ignores ancestor visibility); so the dimension layer is finally wired instead of inert.
- Changed: backported the upgraded `projectionDrawer.js` into `graphics_module_2_topic_2_simple_positions/src/` (merge, not blind copy) — brings the filled-arrowhead dimensioning layer and reconciles the clone's stale `projectPP` comment (PP hinge *local Y / VP∩PP*) to *local X / HP∩PP*, matching that clone's own `main.js` fold.
- Added: Phase 3 dimensioning (BIS SP 46:2003 Type B) — `graphics_module_1_topic_3_points/main.js` `drawCompare()` now draws distHP / distVP / distRP as formal dimensions (extension lines with a ~1 mm gap + ~2 mm overshoot, a continuous narrow dimension line, FILLED 3:1 arrowheads, centered value over a paper break) instead of plain numbers, so the 2D Compare sheet reads as a real drafted drawing.
- Added: `Module2/src/projectionDrawer.js` emits a new `dimensionGroup` — overall width / height + distances from HP / VP for the top and front views, as one 1.0px `--color-ink` `LineSegments2` (extension + dimension lines + open 3:1 chevron arrowheads) with CSS2D `--font-mono` / `--text-xs` labels; returned, resolution-tracked, and disposed inside the existing `dispose()` contract (CSS2D nodes pulled per §3.5). Built in the upright world frame, NOT auto-added to `group` (consumer parents + step-gates it, like `ppGroup`); suppress with `drawDimensions:false`. Inert until wired, so no behavioural drift; the `simple_positions` clone's copy is a pending MERGE (§1.4), not a blind copy (it diverges on PP-hinge comments).
- Changed: DESIGN.md `--color-bench-grey` token (§2.1) gained an explicit AA warning ("CRITICAL: 3.4:1 contrast. Strictly for non-text linework and disabled UI only. Fails AA for text.") so nobody reaches for it as a text colour.
- Changed: `graphics_module_2_topic_2_simple_positions` `.connector-toggle` brought to Module 2 master parity — the old compact checkbox-box chip replaced with the pill toggle (`border-radius:999px`, `min-height:36px`, hidden input, `:has(input:checked)` solid-accent fill, dashed-line SVG icon) and the `:has(input:checked):hover` accent-strong hover added, so the connector control reads as one pill family with the quick-views across both modules.
- Added: Step-3 vertex labels now cascade in on `generate()` — `Module2/src/vertexLabeler.js` + the simple_positions copy fade each `.vlabel` from transparent to full opacity staggered by vertex index (240 ms ease, 35 ms/step, `var(--ease-standard)`), so the annotation set appears one-by-one instead of popping in; reduced-motion opts out, and `setOpacity()` clears the transition so the fold cross-fade stays frame-exact.
- Added: `graphics_module_1_topic_2_spatial_framework` ported the Points `.quad-note` feature — a definition line beneath the step-2 room picker that `uiManager.sync()` rewrites from a new `QUAD_NOTES` table with the selected quadrant's textbook wording ("Above HP, In front of VP", etc.), quiet visual reinforcement of the chosen room.
- Changed: DECISIONS.md ADR-032 amended to record the scrollbar-gutter shift from a "+12px right gutter" (asymmetric raw-pixel pad) to a "centered flat gutter" of `var(--space-3)` sourced from the spacing scale (foundations landed; template_starter is the sweep target).
- Fixed: Phase 3 UI/UX polish — normalized the ADR-032 scrollbar gutter platform-wide so the floating pill floats centered in its track (equal negative space each side); every listed scroll container's `padding-right` collapsed from `calc(base + var(--space-3))` to a flat `var(--space-3)`, matching the master `.card__scroll` reference: `.card__scroll` (points, foundations), `.problem-library__body` (Module 2, points, simple_positions, shell.css, template), introduction's `#shape-rail`/`#anatomy-panel`, plus the same-role step-card scrollers (`#step-card` in spatial_framework, `.step-card` in shell.css).
- Fixed: `graphics_module_2_topic_2_simple_positions` quick-view (Top/Front/Side) buttons brought to the platform standard — the old standalone-bordered-chip style replaced with the master's segmented pill group (panel-tinted rounded container, transparent chips, `font-weight:600`, solid-accent latched state + `.is-active:hover`), and the `.vp-cluster` gap harmonized to `var(--space-2)`; browser-verified base/hover/latched states against the master.
- Changed: `template_starter/index.html` `#sim-viewport` `aria-label` genericized from the orthographic-projection wording to "3D simulation viewport. Drag to orbit." so the boilerplate reads subject-agnostically.
- Changed: MODULE-STARTER.md (§2 boilerplate note + Case C, §3.2 Case A ⚠️, §5.2, §5.3) reconciled to the finalised `template_starter/` — the stale "main.js not yet sanitised / EG leaves still ship / delete them" guidance replaced with the shipped reality (main.js boots a clean empty scene importing only platform leaves; the 5 EG domain leaves + local `DESIGN.md` already removed), so Case A now re-adds the solid imports and Case C fills the empty `rebuild()` seam; two self-contradicting bullets in this same entry reconciled.
- Fixed: `.card__scroll` pinned-nav-footer drift backported from the Module 2 master into `graphics_module_2_topic_2_simple_positions/` (index.html CSS + markup, `stepper.js` scroll-reset retargeted `#step-card`→`.card__scroll`), so the 6-step wizard's Back/Next/Reset stay pinned while the body scrolls; `graphics_module_2_topic_1_introduction/` is the anatomy-gallery adaptation with no wizard, so it was intentionally skipped.
- Changed: `template_starter/` finalized to a clean empty-scene boot — `main.js` rewritten to drop all deleted-geometry/projection/fold/ortho imports and the `initUIManager` wiring (its dock crashes on the placeholder markup), leaving a perspective scene + grid + working stepper + `window.simAPI` + a disposal-contract-only `rebuild()`; `src/terms.js` restored to the Module 2 popover behaviour with an empty `TERMS`; the 5 solids-only leaves (`iShape`/`projectionDrawer`/`meshAnalyzer`/`vertexLabeler`/`problemLibrary`) + local `DESIGN.md` deleted; `CLAUDE.md` swapped for the filled `CLAUDE.module-template.md`; `#fa-symbol` + the empty-state overlay removed from index.html and the quick-view/connector cluster left in place but `hidden` (dormant placeholders). Browser-verified: both pages boot with zero console errors.
- Fixed: slider track harmonized — `--color-track` `#cfc8b8`→`#e0e1e5` across all 7 `:root` sites + DESIGN.md, so the slider groove matches the cool `--color-border` scrollbar pill (the last warm-neutral outlier missed by ADR-040).
- Changed: cool-neutral palette harmonization (ADR-040) — `--color-panel` `#f5f5f5`→`#f0f2f5`, `--color-ink` `#221f18`→`#06070b`, `--color-ink-secondary` `#564e3c`→`#5a5d66`, `--color-border` `#d9d2c3`→`#e0e1e5`, re-derived from the clinical LAB source across all 7 `:root` sites + root DESIGN.md; the ink-blue accent and every HP/VP/PP domain encoding were left untouched, so Quiet-Chrome and Two-Cue still hold.
- Changed: viewport control clusters (`.vp-cluster` / foundations `.vp-controls`) moved from `top: 10%` to a fixed `top: 44px` (the quick-view hit-target height) in Module2, points, simple_positions, foundations, and shell.css, so the top gap mirrors one button height (supersedes the old responsive host-overlay clearance).
- Fixed: horizontal scrollbar flash during the `panelIn` translate — added `overflow-x: hidden` to every step-card scroll container (`#step-card` in Module2/simple_positions/spatial_framework, `.card__scroll` in foundations/points, `.step-card` in shell.css).
- Added: DECISIONS.md ADR-040 records the cool-neutral remap (with the LAB→hex conversion, the rejection of a wholesale shadcn-theme inject, and the re-measured WCAG contrast); root DESIGN.md token table + North Star + Border-Over-Shadow + scrollbar-tint prose synced to the new values.
- Changed: Module 2 master step card adopts the pinned nav footer (matching Foundations/Points) — header + step body wrapped in `.card__scroll` (which now owns `overflow-y` + the ADR-032 scrollbar pseudo-elements), `.card__nav` (Back/Next/Reset) pinned outside it so it stays reachable no matter how tall a step's controls get; `stepper.js` scroll-to-top retargeted from `#step-card` to `.card__scroll`.
- Added: minted `template_starter/` — a dual-mode (guided-stepper + Compare workbench) starter scaffold cut from the Module 2 master with all Engineering-Graphics domain content stripped (7 solid-geometry files deleted, `problems.js`/`terms.js` emptied to stubs, `stepper.js` + rail + step panels reset to 3 placeholders), the Compare-view DOM + CSS ported from the Points topic, and `MODULE-STARTER.md` copied in as its README; its `main.js` shipped initially as a rewrite-me stub, then was finalised the same day to a clean empty-scene boot (see the finalisation entry above).
- Fixed: scrollbar gutter (ADR-032) — added a +12px (`--space-3`) right pad to every floating-scrollbar scroll container platform-wide (13 containers across 7 files: `.card__scroll` + `.problem-library__body` in Module 2/points/template, shell.css `.step-card` + `.problem-library__body`, the simple_positions/spatial `#step-card` scrollers, introduction's `#shape-rail`/`#anatomy-panel`) so the pill floats in its own gutter instead of crowding the text.
- Changed: MODULE-STARTER.md Sections 2/3/5 (+ Preamble & anti-pattern reconciliations) now direct new builds to **duplicate `template_starter/`** — Case A restores the solids from `Module2/`, Case C fills the empty seams — instead of copying `Module2/` and deleting domain files by hand; the doc states honestly what the boilerplate ships stripped vs. what still needs wiring.

## 2026-07-10
- Added: DECISIONS.md ADR-039 records the repository flattening — every topic/module sub-repo's inner `.git` was deleted and everything collapsed into one unified `Simatrix` monorepo (clean-slate history, dropped detached sub-repo histories deliberately) to match the centralized root docs and enable atomic platform-wide commits + centralized git hooks; ARCHITECTURE.md §2 updated to state the platform is now one unified git monorepo rather than separate sub-repositories.
- Changed: Consolidated the whole project into a single git repository and connected it to `github.com/KenwayBlue7/simatrix`; the five subfolders that were each their own separate git repo (Module1, Module2, and three graphics_module topic folders) were flattened into this root repo so everything lives and pushes as one codebase.
- Fixed: Points Compare-split rail controls now sit centred (`justify-content: center` on `body.compare-split #workbench-rail`) instead of hugging the far left with an empty void on the right.
- Fixed: Points `P` label collision — `CHIP_OFFSET.P` y-offset restored 0.44 → 0.60 (`labelLayer.js`) so the `P` chip clears `p′` again in the 3D viewport.
- Changed: DECISIONS.md ADR-021 amended to record that the "single hairline top border" rule is overturned for the Points 2D Compare card (borderless, flows seamlessly into the rail); the seam survives only as the 3D viewport's bottom border.
- Fixed: the 3D→2D fold now SWOOPS square-on to the orthographic answer sheet (ADR-036 code landed, overturning ADR-013's held-angle hold) — implemented on the existing dual-camera stack in the shared engine (`Module1/src/engine.js` `animateFoldSwoop`/`snapFoldSwoop`, driving Module 1 Points **and** Lines) and in the standalone Points topic (`graphics_module_1_topic_3_points/main.js` `swoopToAnswerSheet`); the held-angle movers (`animateFoldHold`/`framePerspectiveToFlat`, plus the topic's `fitPerspectiveDistance`) are deleted, and the reverse fold glides back to the retained perspective pose so `preFoldPose` is no longer stored on those lessons.
- Fixed: Lines 2D Compare drawing scale locked to the static sheet bounds — `sheet2D`'s `SHEET2D_SPAN` is now derived from `SHEET` (`(SHEET/2)*10` = 300 mm per half) instead of a magic 100 mm that the 150 mm True-Length slider overran, so a real mm reads the same on-screen length at any True Length and the worst-case line never overflows the sheet (never auto-zooming).
- Changed: Spatial Framework step-4 card title "Fold (Rabattement)" → "Fold" (the `rabattement` term definition + body copy are unchanged).
- Added: DECISIONS.md ADR-038 (the 2D Compare drawing is a fixed sheet-locked scale, never auto-zooming) + RULES.md §5.19; ADR-013 amended to the binding rule "the fold MUST transition to the orthographic camera, held-angle folds forbidden" and ADR-036 marked implemented (2026-07-10); RULES §5.7/§5.9 synced to the landed function names and the no-`preFoldPose` swoop behaviour.
- Added: Points topic gains a true 50/50 Compare-split workbench (ADR-037) — parity with the Lines workbench (ADR-021), but built in the topic's own standalone architecture (DOM re-parenting of the driver controls into a docked rail + a `body.compare-split` CSS grid), NOT via the shared engine's `initSim({ compareSplit, compareDefaultSize, workbenchControls })` keys — those don't exist in this topic since Points was migrated off `engine.js` (ADR-033). Single-WebGL-canvas invariant preserved (right pane reuses the existing 2D drawing canvas). ADR-021's "Points omits `cfg.workbenchControls`" clause struck; browser-verified with zero console errors.

## 2026-07-09
- Fixed: ADR-032 system-wide scrollbar harmonization — the Module 2 master, both Module 2 topic copies, Foundations, and Spatial Framework scrollbar pills were still tinted accent-blue in code (their 2026-07-02 changelog entries and CSS comments already claimed `--color-border`); WebKit thumb + Firefox `scrollbar-color` retinted to `var(--color-border)` everywhere, `scrollbar-width` confirmed unset on every base rule (Chrome 121+ guard), no hidden scrollbars remain.
- Changed: Points UI polish — scrollbar pills retinted accent-blue → `--color-border` (ADR-032 Quiet Chrome) in the points topic AND Module1 `shell.css` (whose `.step-card` scrollbar was still fully hidden — now the standard floating pill); points step card adopts the Foundations pinned nav footer (inner `.card__scroll` scrolls, footer always visible); distance sliders capped at 40 mm (slider + typed); 3D point sphere reduced 0.16 → 0.12.
- Fixed: Problem Library page, problem cards, `#active-problem` (statement + hints) panel, and the Compare-card frame retinted panel-grey → pure white `--color-paper` in Module1 shell.css, Module2, and the points topic (simple_positions already shipped white), completing the white-content-on-grey-shell mapping; root DESIGN.md token table + §4.2/§5.5/§6/§7 prose synced (panel was still described as "the white surface").
- Fixed: side-panel content areas (Module1 `.step-card`, Module2/points-topic `#step-card`, introduction `#shape-rail`/`#anatomy-panel`) retinted panel-grey → pure white `--color-paper`, so the text/slider/scrollbar zone reads white on the grey panel shell (Phase 1 Final).
- Added: system-wide ortho orbit lock — `enableRotate = false` on every dual-camera ortho OrbitControls pair (Module1 engine + points topic were unlocked; Module2/simple_positions/spatial already were), with a `qv-lock-cue` shake+ring nudge on the latched quick-view chip (spatial: the Unfold button) when a left-drag is attempted in a locked ortho view (Phase 1 Final).
- Fixed: quadrant numerals I–IV (points + spatial_framework label layers) now occlude behind the HP/VP sheets — per-frame `updateOcclusion()` raycast from the live camera hides any numeral a plane fill sits in front of (CSS2D labels bypass the depth buffer) (Phase 1 Final).
- Changed: Phase 1 Corrections — paper/panel tones inverted (`--color-paper` → `#ffffff`, `--color-panel` → `#f5f5f5`) across all 7 shells + DESIGN.md, so the viewport is white and panels are gray (reverses the Part-2 mapping).
- Changed: viewport control clusters (`.vp-cluster` / foundations `.vp-controls`) raised from `top: 15%` to `top: 10%` (Phase 1 Corrections).
- Changed: sliders redesigned minimal — borderless 14px accent dot thumb on a thin 3px pill track (2px paper border + border halo removed); scrollbar thumbs (WebKit pill + Firefox `scrollbar-color`) retinted to the accent to match the slider thumb (Phase 1 Corrections).
- Fixed: quadrant numerals I–IV in Module1/quadrants.js and spatial_framework labelLayer.js moved to the far outer corners `[4.1, ±4.1, ±4.1]`, matching the Points topic (Phase 1 Corrections).
- Fixed: Points topic quadrant numerals (I–IV) pushed from mid-space anchors to the grids' far outer corners (`[±4.1, ±4.1]` at the right end of the fold line) so they no longer overlap point P, p, p′, or the projectors (Phase 1, Part 3).
- Fixed: Foundations Ø30 leader arrowhead was built edge-on to the front annotation plane (back-spread along +Z) and read as a bare line — rotated 90° into the plane so it faces the camera as a filled wedge (Phase 1, Part 3).
- Removed: Foundations "Front View" viewport button + its dead click wiring (the ortho elevation stays reachable via `simAPI`/drag-exit machinery) (Phase 1, Part 3).
- Removed: stale Host-White Exception / `--color-host-white` references in RULES.md §4.11 and §4.15, orphaned by the Part-2 palette reversal — §4.11 now points to `--color-panel` as the white surface (Phase 1, Part 3).
- Changed: system-wide clinical palette code phase (Phase 1, Part 2) — `--color-paper` → `#f5f5f5` and `--color-panel` → `#ffffff` across all 7 shells (Module1/src/shell.css, Module2, the 5 topic copies); `--color-host-white` retired (all consumers repointed to the now-white `--color-panel`); DESIGN.md token table synced to the shipped values and the Part-1 migration warning removed.
- Changed: viewport control clusters (`.vp-cluster` in Module1 shell/Module2/points/simple_positions, `.vp-controls` in foundations) moved to `top: 15%` so host overlays have room above them.
- Added: wizard hide/show toggle (`#wizard-toggle`) + two-state Reset confirm ("Reset everything? · Yes / Cancel") ported to foundations and spatial_framework (spatial_framework also gained its first in-sim Reset button, routed through `simAPI.reset()`); the Reset confirm was also added to the introduction gallery; fold/unfold step buttons in spatial_framework and points now use `.btn--block` full-width.
- Added: DECISIONS.md ADR-036 — restores the 3D-to-2D front-on orthographic camera swoop during the fold, explicitly overturning ADR-013's held-angle revision (`animateFoldHold` retired); RULES.md §5.5–§5.8 rewritten to mandate the swoop (Phase 1, Part 1 — Documentation Reversals).
- Changed: DESIGN.md "The Patient Tutor's Paper" North Star + Host-Integration White Exception, and PRODUCT.md contrast commitments — the warm-paper "deliberately not clinical" framing is retired for the web team's clinical palette (base gray `#f5f5f5`, white `#ffffff`); token table keeps shipped code values with a dated migration note, and WCAG contrast re-measurement is flagged mandatory before the palette code phase ships.

## 2026-07-03
- Added: graphics_module_1_topic_2_spatial_framework — the "Spatial Framework" topic (Quadrants + First-angle combined into one Module-1 subject) is officially BUILT and shipped on the Module 2 orchestrator pattern (ADR-033): the 5-step "Room & Shadow" guided stepper walks the learner from the 3D HP/VP room corner to the flat first-angle layout, reusing the platform's core cinematic fold animation (the 1600 ms `easeFold` rabatment, `anim.js` byte-identical to Module 2's), and the interactive Point P's HP/VP distances are clamped to 1–40 cm so P can never touch a plane or drift out of its chosen quadrant — quadrant changes go only through the explicit select, enforcing the quadrant pedagogy. CSS2D label layer, glossary term popovers, and the parameter dock complete the interactive UI; headless-verified with GL/DOM resources exactly flat across 50 rebuilds. ARCHITECTURE.md §2 + §8 updated to record the shipped status (last stale "scaffolded" reference removed).
- Fixed: graphics_module_1_topic_2_spatial_framework/index.html — closed an ADR-033 scaffolding deviation by wrapping `#step-card`/`#step-rail` in a `.wizard-main` row (card left/`order:1`, rail right/`order:2`) per DESIGN.md §4.5, matching the Module 2 orchestrator layout already proven in Foundations; also added the platform's floating padded scrollbar pill to `#step-card` (ADR-032, DESIGN.md §5.11) and a matching mobile-breakpoint fix (`.wizard-main` back to column at ≤767px) so the row layout doesn't collapse the card at the existing 300px mobile wizard width.
- Changed: PLATFORM-RULES.md §2 — added the wizard row layout (card left/rail right, DESIGN.md §4.5) and the floating scrollbar pill (ADR-032, DESIGN.md §5.11) as explicit ✅ DO / ❌ NEVER checklist rules, so future topics don't ship the same scaffolding deviation.

## 2026-07-02
- Added: graphics_module_1_topic_2_spatial_framework guided-step wizard — the 5-step "Room & Shadow" sequence (src/spatialSteps.js, TERMS merged verbatim from the legacy Module1 quadrant/first-angle glossaries) driven by a Module-2-pattern stepper controller (src/stepper.js) through main.js's injected simController; wizard rail/card markup replaced the placeholder in index.html. Headless-verified per ADR-019 (clean boot, fold gate, single reset path); 3D geometry leaves still pending. ARCHITECTURE.md §2 entry updated from SCAFFOLDED to IN BUILD.
- Changed: DOCUMENTATION-SYSTEM.md — reconciled the methodology doc with the platform/module-template split: added PLATFORM-RULES.md and CLAUDE.module-template.md as the two extension files a project adds once it outgrows one product family; split "How CLAUDE.md Connects Everything" into Case A/B (five root docs) vs Case C (three root docs) pointer blocks; added the meta.json lowercase-difficulty rule to the Reuse Checklist; added a new "Sharing the System With New Contributors" section covering what to hand a teammate starting a new subject module, the day-one starter-prompt and audit-then-harmonise onboarding patterns, and the platform-wide bug fix pattern (using the meta.json capitalisation fix as the worked example); and updated the Verification Checklist's CLAUDE.md-completeness item to check for the correct Case A/B vs Case C root doc pointer block instead of assuming one universal block.
- Fixed: meta.json difficulty field capitalisation — changed to lowercase across all 6 meta.json files to match backend contract; added PLATFORM-RULES.md §1.11a to prevent recurrence.
- Changed: MODULE-STARTER.md — reconciled Section 5 (Case C — a whole new subject module) with `PLATFORM-RULES.md`/`CLAUDE.module-template.md`: Case C no longer copies `Module2/`/`Module1/` wholesale or shares the root `ARCHITECTURE.md`/`DECISIONS.md`/`RULES.md` (that guidance was written before this session's platform/module-template split and had gone stale); it now builds a fresh platform skeleton and writes its own three docs, with its own ADR numbering starting at ADR-001. Added scope-clarifying notes to Sections 1, 2, 7, 8, and 9 so a Case C reader doesn't follow Case A/B-specific instructions (root-doc section citations, `iShape.js`, `topic_N` numbering) that don't apply to them; Sections 3, 4, and 6 (Case A/B mechanics) are unchanged.
- Added: CLAUDE.module-template.md — a starter `CLAUDE.md` for a brand-new Simatrix subject module (not Engineering Graphics), carrying only the platform-wide contract: the project-wide documentation block (pointing at root `DESIGN.md`/`PRODUCT.md`/`PLATFORM-RULES.md`, deliberately not the EG-specific root `ARCHITECTURE.md`/`DECISIONS.md`), the Session Digest Protocol, a "keeping your own documents current" block, and a placeholder architecture section with an explicit instruction that a new subject must author its own `ARCHITECTURE.md`/`DECISIONS.md`/`RULES.md` (the last built on `PLATFORM-RULES.md`) before writing simulation code — so a future contributor isn't handed the Module1/Module2-specific `CLAUDE.md` as their starting point by mistake.
- Added: PLATFORM-RULES.md — extracted the subject-agnostic subset of RULES.md (platform/runtime contract, all of §4 UI & Visual, the generic half of §7 Cross-Module Harmony renamed "Cross-Subject Harmony," all of §8 Documentation, and the matching §9 anti-patterns) so a new engineering-discipline module has a ready checklist without wading through Engineering-Graphics-specific rules (master/deploy map, 3D scene internals, camera behaviour, answer-validation specifics); any rule that only made sense with a specific file or lesson name attached (`anim.js`, `uiManager.js`, "Lines"/"Points," etc.) was either generalized to its underlying principle or left out.
- Changed: ARCHITECTURE.md §2 — registered graphics_module_1_topic_2_spatial_framework in the codebase map (SCAFFOLDED, NOT BUILT) and updated the §2 cross-family note + §8 to reflect ADR-033: the Module-1-subject-on-Module-2-pattern hybrid is no longer "the only place the families mix" but the platform-wide direction, with Spatial Framework as its first migrating topic.
- Added: graphics_module_1_topic_2_spatial_framework/src/spatialData.js — the topic's pure data layer (point distances from HP/VP, quadrant enum + sign table, position/quadrant resolvers), adapted from Module1/src/pointData.js with zero engine.js dependency, per MODULE-STARTER.md §7's data-layer-first build order.
- Added: DECISIONS.md ADR-033 — Module 1 fully adopts the orchestrator pattern platform-wide: Quadrants, First-angle, Points, and Lines are migrating off the shared-`engine.js` architecture onto Module 2's orchestrator + leaf-module pattern (ADR-007), the same pattern Foundations proved (ADR-029); Quadrants + First-angle ship combined as one new topic, "Spatial Framework." This fully supersedes ADR-011, whose status was updated to point here (ADR-029 remains on record as the first, Foundations-only partial supersession).
- Added: graphics_module_1_topic_2_spatial_framework — scaffolded the platform skeleton for the new "Spatial Framework" topic (Quadrants + First-angle combined) per ADR-033: import map, root design tokens (including the HP-teal/VP-amber/PP-violet projection encodings this topic will need), `meta.json`, and a `window.simAPI`-contract `src/main.js` that boots a clean, empty, orbitable Three.js scene. No lesson content (HP/VP planes, point, fold, guided steps) yet — CLAUDE.md is marked SCAFFOLDED, not BUILT, and lists the open work.
- Changed: DESIGN.md — codified the cross-module "Simatrix Feel" from a code audit of the master (Module2): added §4.5 (wizard is a right-hand vertical column beside the viewport, with a strict background mandate — viewport `--color-paper`, panel `--color-panel`) and corrected §5.5 to note `.brand`/`.chapnav` are Module-1-only (Module 2's wizard is the `#active-problem` header + `.wizard-main` row).
- Changed: DESIGN.md §3.2 — split the "Step X of N" step eyebrow out of the Atkinson-700 Label role into its own IBM Plex Mono 400 micro-label role, matching the actual `.card__eyebrow` CSS (the doc had mis-described it).
- Changed: DESIGN.md §5.1 — mandated that buttons keep the inherited Atkinson 400 weight (`font: inherit`, never 700 or faux-bold) and that the ghost Reset is guarded by the inline two-state confirm.
- Added: RULES.md §4.19 (Reset must use the two-state "Reset everything? · Yes / Cancel" confirm; single-click wipe forbidden) and §5.18 (perspective↔orthographic view changes must use the `projectionMorphK` dual-camera matrix morph, never a one-frame camera swap), plus matching Section 9 anti-patterns — so the polished interaction/animation behaviours are now enforceable, not just implemented. and graphics_module_2_topic_2_simple_positions/src/meshAnalyzer.js — backported the degenerate-triangle safeguard from the Foundations clone (drop zero-area triangles via the new DEGENERATE_NORMAL_EPS floor instead of keeping a (0,0,0)-normal face), so the coplanarity test can no longer be poisoned into drawing phantom "spiderweb" seams; all three meshAnalyzer.js copies are now byte-identical (RULES §1.5 backport-by-merge, §7.10).
- Added: DECISIONS.md ADR-030 — records integrating three-mesh-bvh through the CDN import map to accelerate the non-convex occlusion raycaster, resolving the CPU bottleneck while preserving the no-build contract (ADR-001).
- Added: DECISIONS.md ADR-031 — records replacing the ~100ms occlusion debounce timer with real-time requestAnimationFrame throttling (dirty-gated on camera movement), enabled by the BVH, so hidden lines update live as the part orbits.
- Changed: RULES.md — added §2.20 (pin three-mesh-bvh in the import map like three), §3.29 (solids must be manifold — no overlapping extrusions — to prevent raycast explosions), §3.30 (meshAnalyzer.js must drop degenerate triangles), §3.31 (BVH is derived state — rebuild/dispose it in rebuild()), §3.32 (rAF-throttle heavy on-orbit updates, never debounce), and §7.10 (keep meshAnalyzer.js byte-identical), plus matching Section 9 anti-patterns.
- Changed: ARCHITECTURE.md §7 — noted three-mesh-bvh as a second pinned import-map dependency used by the Foundations topic's occlusion raycaster (the master and Module-2 clones don't load it).

## 2026-06-29
- Added: graphics_module_1_topic_1_foundations — built the full sim (index.html shell + src/main.js orchestrator, annotations.js, labelLayer.js, stepper.js) on the Module 2 orchestrator pattern, completing the planned refactor: an orbitable 3D bearing block that teaches the four BIS line types with a live line-of-sight (raycast) hidden-line classifier, so visible (Type A) edges turn dashed-hidden (Type E/F) as the student spins the part, plus authored Type G centre lines and Type B dimensions revealed by a guided 4-step wizard; single rebuild() pipeline with the full WebGL + CSS2D disposal contract, window.simAPI, and a "Front View" quick-view.
- Added: DECISIONS.md ADR-029 — records that graphics_module_1_topic_1_foundations adopts the orchestrator pattern and retains meshAnalyzer.js plus a per-edge line-of-sight occlusion classifier, formally superseding ADR-011 for this topic only (ADR-011 still governs Module 1's other points/lines lessons; its status now points to ADR-029).
- Changed: graphics_module_1_topic_1_foundations/CLAUDE.md — flipped status from PLANNING to built and reconciled the planned steps.js name to the shipped stepper.js.
- Fixed: graphics_module_1_topic_1_foundations/index.html — added a global `[hidden] { display:none !important }` reset; without it the `.sim-fallback` / `#mobile-notice` / `.btn` rules (which set an explicit `display`) tied the UA `[hidden]` rule on specificity and, being later in source order, won — so the boot fallback overlay permanently covered a perfectly-booted sim (caught in the browser smoke test). Also moved the orbit hint to the bottom-left so it never overlaps the centred dimension value.
- Fixed: graphics_module_1_topic_1_foundations/src/main.js — the "Front View" framing now fits the whole DRAWING (block + the Type B dimensions that sit below/right of it) and accounts for those annotations lying on the front face (closer to the camera, so they project larger); previously the L and H dimensions were clipped at the viewport edges at Step 4.
- Changed: ARCHITECTURE.md §2 and §8 — documented `graphics_module_1_topic_1_foundations` as the one cross-family hybrid (a Module-1 subject built on Module 2's orchestrator pattern with a copied meshAnalyzer.js), and noted the "Module 1 has no edge analyzer" statement no longer holds for this topic (ADR-029).

## 2026-06-28
- Fixed: graphics_module_2_topic_2_simple_positions/index.html `<title>` — changed the stale "Orthographic Projection of Solids — Simatrix" (inherited from the master) to "Simple Positions" so it matches `meta.json.title`, resolving the ADR-026 known violation (browser-tab text and screen-reader accessible name now agree with the catalog); updated ADR-026 status to fixed and reworded MODULE-STARTER.md §9 to cite it as a historical example.
- Changed: graphics_module_2_topic_1_introduction/index.html and Module2/index.html `<title>` — stripped the " — Simatrix" suffix so each single-sim payload's `<title>` is now identical to its `meta.json.title` ("Introduction to Solids", "Orthographic Projection of Solids") per ADR-026/RULES §1.12; clarified in RULES §1.12 that the rule governs single-sim payloads, so Module 1's seven per-lesson page titles (one shared `meta.json`) are by design, not violations.
- Added: DECISIONS.md ADR-024…ADR-028 — recorded five decisions MODULE-STARTER.md had surfaced but the ADR log never captured: the `graphics_module_<M>_topic_<K>_<slug>` folder-naming convention (ADR-024), the 2D-vs-3D template-choice rule for new subject modules (ADR-025), the `<title>`-must-match-`meta.json.title` rule (ADR-026), `iShape.js` being an adapt-not-copy file (ADR-027), and new topics consuming the root DESIGN.md/PRODUCT.md instead of local copies (ADR-028), so these now-enforced rules have a documented why.
- Changed: RULES.md Section 1 — added §1.10–§1.14 (folder naming, template choice, `<title>`/meta.json match, `iShape.js` adapt rule, root-doc consumption) and three matching Section 9 anti-patterns, turning the new ADRs into enforceable do/don'ts; flagged Topic 2's live `<title>` mismatch.
- Changed: MODULE-STARTER.md — wired its Section 9 iShape.js/`<title>`/per-topic-doc mistakes to the new ADRs and rules, and updated §5.1/§5.4 (and the §1 DECISIONS takeaways) which had said the 2D-vs-3D and folder-naming rules were unrecorded — they are now ADR-025/ADR-024.
- Added: MODULE-STARTER.md (repo root) — a from-scratch, code-verified playbook for creating a new sim (new Module 2 topic, new Module 1 lesson, or new subject module), with byte-identical file lists confirmed by md5sum, a file-by-file copy/adapt/create breakdown, and a pre-ship verification checklist; every step cites its RULES/ADR/DESIGN/ARCHITECTURE source.
- Changed: all four module CLAUDE.md files (Module1, Module2, graphics_module_2_topic_1_introduction, graphics_module_2_topic_2_simple_positions) — added a "Project-wide documentation" block near the top pointing to the root ARCHITECTURE/DECISIONS/RULES/DESIGN/PRODUCT docs, so cross-module work starts from the shared root docs.
- Added: PRODUCT.md (repo root) — one consolidated, code-audited platform product contract merged from the two former byte-identical per-module PRODUCT.md files, so the persona/anti-references/principles/accessibility commitments have a single source of truth.
- Removed: PRODUCT.md from Module1/ and Module2/ — the per-module copies; their content now lives in the root PRODUCT.md.
- Changed: Module1/CLAUDE.md and Module2/CLAUDE.md — repointed the @PRODUCT.md strategic-context references to ../PRODUCT.md.
- Changed: RULES.md §7.3 — replaced "keep PRODUCT.md identical between modules" with a single-root rule (ADR-023), mirroring §4.15 for DESIGN.md.
- Changed: ARCHITECTURE.md §7/§9.4 — recorded that PRODUCT.md is now one root file, no longer duplicated per module.
- Added: DECISIONS.md ADR-023 — records the PRODUCT.md centralization (parallels ADR-022) plus the audit's two flagged gaps (Shift+arrow finer steps unimplemented; Module 1 mobile-notice wording diverges from the "best on desktop" contract).

## 2026-06-27
- Added: DESIGN.md (repo root) — one consolidated, code-audited platform design system merged from the four former per-module DESIGN.md / DESIGN.shared.md files, so the visual contract has a single source of truth instead of four drifting copies.
- Removed: DESIGN.shared.md from Module1/, Module2/, and graphics_module_2_topic_2_simple_positions/ — the per-module copies that had drifted; their content now lives in the root DESIGN.md.
- Changed: Module1/CLAUDE.md and Module2/CLAUDE.md — repointed all shared-design-system references to ../DESIGN.md (away from the deleted DESIGN.shared.md / each module's local DESIGN.md).
- Fixed: design docs vs. code — corrected two stale claims to match the code (button press is scale(0.97), not a 1px nudge; the auto-zoom dolly is 500ms/easeStandard, not 520ms/easeCamera) and recorded the Lines construction-aid tokens the docs never mentioned.
- Changed: RULES.md — updated §4/§7 citations to DESIGN.md, replaced the obsolete "keep DESIGN.shared.md identical" rules with a single-root rule, and added §4.17 (scale(0.97) press) and §4.18 (solid-fill is the canonical fill token, not geometry-fill).
- Added: DECISIONS.md ADR-022 — records the design-system centralization and supersedes ADR-010 (which had accepted per-module DESIGN.shared.md duplication only until a shared root existed); ADR-010 marked Superseded.
- Changed: ARCHITECTURE.md §7/§8/§9 — marked the DESIGN.shared.md drift and per-module design-doc duplication as resolved/partially-resolved (pointing to ADR-022) and corrected the stale "root has no .md files" note.
- Added: RULES.md — the enforcement checklist (120 ✅ DO / ❌ NEVER rules across 9 sections, each traceable to an ADR/ARCHITECTURE/DESIGN/CLAUDE source) that turns every documented decision into an actionable do/don't; it fulfills the missing `SIMATRIX-UI-STANDARDS.md` reference and states the Module2-is-master deploy map explicitly.
- Changed: Module1/CLAUDE.md and Module2/CLAUDE.md — repointed the broken `../SIMATRIX-UI-STANDARDS.md` link to the new `../RULES.md` so the cross-module standards reference resolves.
- Added: DECISIONS.md — an ADR log recording WHY key choices were made (20 entries spanning platform delivery, the 3D engine, shape system, cross-module sync, camera behavior, validation, and naming), including five deliberately-reversed decisions, so future contributors don't accidentally undo them.
- Added: ARCHITECTURE.md — a first whole-repo architecture map (what Simatrix is, codebase map, Module 1 & Module 2 component breakdowns, data flow, the iframe boundary, shared vs. divergent code, and known structural issues), written from reading the source and saved session memory.
