# Changelog

## 2026-07-20
- Changed: `--color-vp-line` darkened `#bc5d1e → #b25718` (platform-wide AA promotion, ~4.92:1 on paper) — `index.html` `:root`.

## 2026-07-10 (step-4 title copy)
- Changed: the step-4 card title "Fold (Rabattement)" → "Fold" — the `rabattement` <button class="term"> definition and the step body copy that uses it are unchanged. (`src/spatialSteps.js`.)

## 2026-07-09 (ADR-032 scrollbar retint)
- Fixed: the `#step-card` scrollbar pill was still tinted `--color-accent` (the block comment already described the border token) — WebKit thumb + Firefox `scrollbar-color` retinted to `var(--color-border)` per ADR-032 Quiet Chrome.

## 2026-07-09 (Phase 1 Final — orbit-lock cue on the flat sheet, numeral occlusion)
- Added: attempted left-drag on the rotate-locked flat answer sheet now nudges the "Unfold back to 3D" button (`qv-lock-cue` shake + focus ring) so the learner reads "unfold to orbit" instead of a dead drag — the ortho pair was already rotate-locked. (`src/main.js`, `index.html`.)
- Fixed: the I–IV quadrant numerals no longer read through the HP/VP sheets — a per-frame `labelLayer.updateOcclusion()` raycasts camera → numeral against the sheet FILL meshes (Line2 grids excluded: they extend Mesh but their raycast needs `Raycaster.camera`) and hides any occluded numeral; runs after `renderer.render`, before the CSS2D re-projection. (`src/labelLayer.js`, `src/main.js`.)

## 2026-07-06 (Step 4→5 cage-snap fix — Q3 frustum render order)
- Fixed: the grid "cage" popped onto the Q3 frustum in one frame at the end of the 4→5 cross-fade (and popped OFF at the start of 5→4) — the fading body drew in the transparent pass AFTER the sheet grids and painted over them, then flipped opaque at k=1 and let the grids composite on top all at once; the Q3 body now carries `renderOrder = -3` (`src/frustums.js`) so it draws beneath the sheet fills/grids while translucent, making the transparent↔opaque flip visually seamless (the material opacities were never the problem — probed smooth on both directions).
- Verified: headless CDP — per-frame material-opacity probe (extension grids ride easeFold 0→0.55 in lockstep with the hinge, both directions), before/after screenshot crops (cage now grows over the Q3 body progressively; 1719 ms ≙ 1884 ms ≙ settled), and the GL-flat check: live GL objects + `.lbl` DOM exactly flat across 25 Step 4↔5 round trips (77/4/6/27, 9 labels), 25 `simAPI.reset()` (45/4/4/17, 5 labels), and a post-churn re-walk, 0 console errors.

## 2026-07-05 (Phase 2 polish — amber lines corrected to space-vs-plane, frustum projectors removed)
- Fixed: the 3D-space VP projector `P→p′` is dashed amber again (`src/point.js`) — the earlier "all VP linework solid" override wrongly made a pictorial 3D projector solid; now ONLY the on-VP `p′→fold-line` connector stays solid (echoing the amber sheet border), so the dashed-3D-projector rule is honoured while the on-plane exception is kept narrow.
- Removed: all body→view projectors from the Step 5 frustum illustration (`src/frustums.js`) — the dashed body→HP foot-tracker and the solid body→VP line, plus the now-dead `setSegment` helper and `hpProjectors` tracking; the frustums simply float above their 2D top/front views for a cleaner first-angle read.
- Verified: headless CDP GL/DOM leak-check — live GL objects + `.lbl` DOM exactly flat across 50 `simAPI.reset()` (45/4), 20 Step 4↔5 round trips (step5 77/8, step4 87/7), and 12 fold/unfold cycles (87/7), with 0 JS errors/exceptions.

## 2026-07-05 (Grid-fade lifecycle fix — independent four-rooms extension, no corner artifacts)
- Fixed: Step 1 → 2 snapped the extended quadrant sheets in — the whole planes leaf was re-faded from 0, blinking the already-visible Quadrant-I corner; the beyond-fold extension now fades in on its own while the corner stays put (Task 1).
- Fixed: Step 2 → 1 flickered/Z-fought along the grid edges — the old extended leaf was dissolving in the graveyard on top of the full-opacity fresh half-sheets, double-drawing the shared corner; the shrink now fades only the extension out with no overlapping leaf (Task 2).
- Changed: `src/hvPlanes.js` now ALWAYS builds the beyond-fold extension geometry and exposes `setExtendedOpacity()` (core vs. extension are separate composable fade buckets); `src/main.js` drives Step 1↔2 grow/shrink purely by fading that extension (`fadeExtension` + `planesExt`), so planes are no longer stolen into the fade-out graveyard — the shared corner can never re-fade or double-draw.
- Verified: headless CDP GL-flat check — live GL objects exactly flat (Texture 4 / Buffer 45 / Program 4 / VAO 17) across 50 Step 1↔2 round trips and 50 `simAPI.reset()`, 0 console errors; grow/shrink screenshots eyeballed (extension fades progressively, corner rock-steady, shrink lands byte-identical to the clean Step 1).

## 2026-07-05 (Polish batch — bidirectional grid fade, centered fold buttons, solid VP projectors, boxed quadrant labels)
- Fixed: navigating Step 2 → Step 1 (or 2→3 / 5→4) snapped the extended quadrant sheets back to the room corner — `applyView()` in `src/main.js` now steals the shrinking planes leaf into the fade-out graveyard and dissolves it while the fresh half-sheets appear beneath, so the extra regions fade OUT smoothly (the mirror of the existing grow fade-IN).
- Changed: the "Fold HP down" / "Unfold back to 3D" buttons now sit centered — added `justify-content: center` to `.fold-actions` in `index.html`.
- Changed: the 3D VP projectors are now SOLID amber instead of dashed (`src/point.js` P→p′ + its fold-line connector, `src/frustums.js` body→VP), an explicit curriculum-owner override of the Two-Cue / dashed-3D-projector rules (commented at both call sites); `computeLineDistances()` is now skipped for solid lines in both leaves.
- Changed: the I–IV quadrant numerals now sit in a solid `--color-paper` box with a `--color-border` edge (`.lbl--quad` in `index.html`) so they pop clearly off the dense grid cage; active/inactive state stays cued by text colour + weight.
- Verified: headless CDP GL-flat check — live GL objects + `.lbl` DOM exactly flat across 50 `simAPI.reset()` (42/4), 50 Step 1↔2 round trips (42/4), and 50 Step 4↔5 round trips (106/8), with 0 console errors; step 2/3/4 screenshots eyeballed (boxed numerals, solid amber VP linework beside the still-dashed teal HP projector, centered fold button).

## 2026-07-04 (Smoother transitions — opacity cross-fades + auto-unfold on back-nav)
- Added: `setOpacity(k)` to `src/hvPlanes.js`, `src/point.js`, and `src/frustums.js` (each scales its own materials from their target opacities; the frustum's opaque body only turns transparent WHILE fading, so the settled "cage" look is unchanged) — the leaf-level hook the orchestrator needs to fade geometry in/out.
- Added: transition fades in `src/main.js` — a `fadeState` (per-leaf opacity multiplier re-stamped after every `rebuild()` so a mid-fade rebuild never snaps) plus an always-disposed fade-out "graveyard"; `applyView()` now fades the HP/VP planes UP from 0 when they first appear or grow past the fold line (Task 1: the Step 1→2 pop), and cross-fades the point rig ↔ frustums across the 4↔5 boundary (Task 2: dissolve the outgoing leaf, fade the incoming one in) — all synced to the hinge's `easeFold`/FOLD_MS when a fold is swinging, a lighter `easeDraw` reveal otherwise. Fades run only on step transitions, never on slider `commit()`s.
- Fixed: navigating back from the folded Step 4 to a pre-fold step (2 or 3) left the corner stuck flat — steps 1–3 now carry `foldPose: 'unfolded'` (`src/spatialSteps.js`) and `applyView()` clears `folded` on arrival, so the existing `driveFold`/`restorePerspective` machinery animates a smooth reverse fold + camera glide back to 3D instead of a snap (Task 3).
- Verified: headless CDP leak-check (shipped module, cache disabled) — live GL resources + `.lbl` DOM exactly flat (42 / 4) across a full 1→5→1 walk, 50 cross-fade round trips, and 50 `simAPI.reset()` cycles, and flat across 25 slider rebuilds; the 4→2 auto-unfold lands correctly; 0 console errors/exceptions; step + mid-fade screenshots eyeballed (planes reach full opacity, frustum bodies opaque with the cage at rest, the 4→5 cross-fade shows point-out/frustum-in together).

## 2026-07-04 (True orbital quadrant walks — camera physically enters every room)
- Fixed: Q2/Q3 camera poses never crossed the VP — `CAMERA_POSE` in `src/main.js` now places each Q1–Q4 pose PHYSICALLY INSIDE its own quadrant (position's y/z signs = the quadrant's sign pair: Q2 `(-8, 4.9, -8)`, Q3 `(-8, -4.9, -8)`), so east↔west walks genuinely go behind the wall.
- Changed: `flyCamera()` re-rewritten from the Bezier "hop over" back to a TRUE spherical orbit — the camera's offset from `controls.target` is direction-slerped (quaternion) with radius + pivot lerped, so every room walk sweeps a circular arc around the point, crossing the VP (±Z), the HP (±Y), or both (diagonals) around the fold line's X end.
- Verified: offline path simulation (all 12 ordered quadrant pairs cross exactly the expected planes, outside the 9×9 sheets, radius exact) + live headless CDP (X/Y end-mark mirror-flip proves the behind-the-wall landings, fold/showcase round trip intact, 0 console errors).

## 2026-07-04 (Visual tweaks — closer camera, calmer grid, fold-fade label fix, arcing flights)
- Changed: pulled the camera ~30% closer to the point/planes (`CAMERA_POSE` default + Q1–Q4 in `src/main.js`) so P and its projectors read large in frame; `showcase` (step 5) left wide so it still holds both frustums and all four extended sheets.
- Changed: calmed the plane "cage" — grid divisions dropped 48 → 24 per sheet (`GRID.divs` in `src/hvPlanes.js`) so the lines no longer buzz/moiré against the fat borders and chips.
- Fixed: the 3D 'P' chip crossing squarely over the 2D 'p′' chip during the fold — the 'P' label now sits higher and further toward the viewer (`CHIP_OFFSET.P` in `src/labelLayer.js`) AND fades to 0 as the corner flattens (`pFoldFade`, driven each frame by `setFoldAngle`), so the flat answer sheet reads as just p / p′.
- Changed: `flyCamera()` rewritten to follow a `THREE.QuadraticBezierCurve3` through a lifted, pushed-out control point (UP over the planes + OUT around the X fold-line axis), so room-to-room walks physically hop OVER the VP wall / HP floor instead of sweeping awkwardly across the corner.

## 2026-07-04 (Final refinements — dual-camera 2D fold, orbital flights, projector fix, copy/terminology)
- Added: dual-camera "flatten to 2D" in `src/main.js` (ported from `Module2/src/main.js`) — an `OrthographicCamera` + its own locked-rotate `OrbitControls`, a `projectionMorphK` matrix blend stamped each frame (`applyProjectionMorph`), and `swoopToAnswerSheet()` / `restorePerspective()` / `engageOrtho()`; the step-4 fold now BOTH swings the HP flat AND swoops the camera square-on while the projection morphs perspective→orthographic, so the folded corner reads as a true flat engineering drawing (front view above the XY line, top view below). Unfolding / navigating back to 3D glides back via `restorePerspective(true)`.
- Changed: `flyCamera()` now sweeps on a SPHERICAL arc — the camera's offset from the origin is slerped (quaternion) with its radius lerped, so quadrant transitions physically swing *around* the dihedral divider instead of cutting a straight chord through the corner.
- Changed: camera vantage moved to the top-left of Quadrant I near the 'X' fold-line end (`CAMERA_POSE` now uses negative-x poses, e.g. default `(-12, 8, 12)`), matching the 'X' end mark at x = −4.85.
- Fixed: dashed projectors visually detaching from Point P on some Linux GL drivers — `src/point.js` now starts both the static VP projector and the dynamic HP projector 0.20 units off P (`SPHERE_GAP`, at the sphere surface), so the dash pattern begins cleanly past P's marker and never overlaps the sphere in the shader.
- Changed: `src/hvPlanes.js` grid densified from ~9 to 48 divisions per sheet (`GRID.divs`), packing the translucent "cage" much tighter.
- Changed: terminology "rabatment" → the formally correct "rabattement" across `src/spatialSteps.js` (term label + definition + step title + body) and the `index.html` rail label (the `data-t="rabatment"` binding key is deliberately unchanged); the Step-2 panel title "Move point P into a room" → "Move point P amongst Quadrants".
- Verified: headless CDP leak-check (Node built-ins, shipped module, cache disabled) — GL live resources + `.lbl` DOM exactly flat across 50 `simAPI.reset()` cycles and 12 fold/unfold cycles, and back to baseline after reset; a second probe drove the full 1600 ms swoop→ortho→restore plus the 4→5→4 round trip with **0 console errors / exceptions**.

## 2026-07-04 (Polish pass — step-5 frustum illustration, camera flights, UI/unit cleanup)
- Added: `src/frustums.js` — step 5 now shows the classic textbook figure: cone frustums in Quadrants I and III (Q3 the point-reflection, big end up) with concentric-circle top views on HP, trapezoid front views on VP, and dashed projectors; the Point P rig stands down via view flags alone (zero `point.js` changes), and step 5 opens the hinge (`foldPose: 'open'`) without un-doing step 4's fold ✓.
- Added: camera pose flights in `src/main.js` — a named pose table (default / Q1–Q4 / showcase) flown with 900 ms `easeCamera` tweens on quadrant walks and step changes; a user drag cancels the flight, a pose-equality guard stops self-tweens, and reduced motion snaps.
- Added: success toast (`#sim-toast`, ported from the Topic 1 sibling) — fires once on first reaching step 5, merged into the single screen-reader announcement, re-arms after `simAPI.reset()`.
- Changed: the VP sheet border is now SOLID amber — explicit user override of the Two-Cue dash pairing for the sheet border only (documented in `hvPlanes.js`; the P→p′ projector linework keeps its dashes) — and the plane grids densified to a "cage" (opacity .55 / fade .60) so the Q3 frustum reads through the sheets.
- Changed: the mid-line `XY` chip became `X` / `Y` end marks on the fold line (`labelLayer.js`), and the quadrant-numeral highlight now lights only while P is on stage (no misleading lit "I" on step 5).
- Changed: all units relabelled cm → mm (pure relabel, magnitudes unchanged — 1 world unit = 10 mm, ADR-018) across the dock copy, aria strings, and code comments.
- Removed: the dock's Quadrant `<select>` (`#field-quadrant`, its `uiManager.js` wiring, and the now-dead `lockQuadrant` view flag) — step 2's room buttons are the one quadrant control.
- Verified: headless CDP per ADR-019 (shipped module, cache disabled) — 29/29 green: camera tweens proven mid-flight (sampled mid-pose ≠ both endpoints, then settles) plus the no-self-tween guard, fold gate + the 4→5→4 folded-flag round trip, toast fire/merge/re-arm, and label DOM + live GL resources exactly flat across 50 step-5-resident rebuilds AND 50 step-4↔5 rail round trips AND 50 slider rebuilds; reduced-motion pass green (camera + fold snap, toast still shows); zero console errors; step screenshots eyeballed against the textbook figure.

## 2026-07-03 (Dock removal + full-height step rail — Module 2 convergence)
- Removed: the "parameter dock" concept — `index.html` drops the `.param-controls` section and its bespoke CSS (plus the `.dock__title`); the Point P sliders + quadrant select now live in a native Module-2 control group (`#controls` → `.dock__group` / `.dock__group-title` / `.dock__note`), so they read as step content, not a bolted-on panel.
- Changed: `src/uiManager.js` — the Point P group is now revealed by progressive disclosure (`#controls.hidden` toggled by the step's `showPoint`): hidden on step 1, shown steps 2–5; within it the quadrant select still greys out on the quadrant-locked steps. Bindings untouched — every control is still found by its own id.
- Fixed: `index.html` step rail — replaced the `<nav><ol class="rail">` wrapper with a bare `<ol id="step-rail">` and ported the family-standard rail CSS byte-for-byte (34px markers, `justify-content: space-between`, `::before` spine `top:30px`/`bottom:30px`), so the five markers now spread across the full card height instead of bunching at the top; mobile reflows the spine to a horizontal strip like Topic 1.
- Verified: dependency-free static check (42/42 green) — all `uiManager`/`stepper` id + selector bindings resolve, no `.param-controls`/`.dock__title`/`#param-dock` left, rail CSS matches the master; then live in Chrome — clean boot, rail height == `.wizard-main` height (573px), `#controls` hidden on step 1 and revealed step 2+, slider→field two-way sync fires, quadrant lock on steps 3–5, rail states (✓/current/upcoming) correct, zero console errors.

## 2026-07-03 (Step-rail button layout — narrow-rail fix)
- Fixed: `index.html` CSS — `.rail__btn` now stacks the step label *below* its number marker (`flex-direction: column; align-items: center; text-align: center`) to match the Module 2 master, so long titles like "First-Angle Setup" no longer sit beside the marker and blow out the vertical rail's width; `.rail__label` gained a `max-width` (matching the master) so those titles wrap instead of stretching the rail.
- Added: `../PLATFORM-RULES.md` §2.22 and `../RULES.md` §4.20 — a ✅ DO requiring `.rail__btn` to use `flex-direction: column` (label below marker, keeps the rail narrow), cited to `DESIGN.md §5.6`; the two rulebooks stay in sync.
- Verified: dependency-free headless check (15/15 green) — `<style>` braces balanced, `.rail__btn` carries the four column-layout declarations with no stale `text-align: left`, `.rail__label` is width-capped, and every rail selector `stepper.js` queries (`#step-rail`, `.rail__item`, `.rail__marker`, `.rail__btn`, `.rail__label`) still resolves to markup — no controller binding lost.

## 2026-07-03 (Param-control relocation + step-name trim — UI regression fix)
- Fixed: `index.html` — moved the Point P controls out of the standalone `#param-dock` card (a disconnected sibling below the wizard row) and INTO `#step-card` as a `.param-controls` divider block above the Back/Next nav, so the sliders read as part of the current step (Module 2 card layout); the empty `#param-dock` wrapper is removed. Bindings were untouched — `uiManager.js` targets each control by its own id, not the wrapper.
- Fixed: `index.html` CSS — repointed the old `#param-dock` rule to `.param-controls`, restyled from a nested paper card (which would double the border inside the card) to a top-rule divider matching the existing `.step-panel` blocks.
- Changed: `src/spatialSteps.js` + `index.html` rail labels — trimmed the leading "The " from all 5 step names (Reference Planes / Four Quadrants / First-Angle Setup / Fold (Rabatment) / Standard) so the step rail no longer overruns its width; the card titles and rail labels stay mirrored.
- Verified: headless jsdom (28/28 green) — `#param-dock` gone, all 8 relocated control ids resolve inside `#step-card`, `uiManager` two-way sync + quadrant + disable/lock paths all fire, `stepper` renders the trimmed titles and leaves the persistent `.param-controls` visible, zero console errors.

## 2026-07-03 (Wizard DOM convergence with the Module 2 orchestrator pattern)
- Changed: `index.html` — wrapped `#step-card` and `#step-rail` in a new `.wizard-main` row (`display: flex; flex-direction: row`), with the card pinned `order: 1` (left) and the rail `order: 2` (right), per `DESIGN.md §4.5`. `#param-dock` is unchanged — still stacked vertically outside `.wizard-main`, inside the `#wizard` column. This was a deliberate DOM restructure to close a scaffolding deviation from ADR-033 (this topic must strictly match Module 2's wizard layout, already proven in `graphics_module_1_topic_1_foundations`), not a visual redesign.
- Added: `index.html` — the platform's floating padded scrollbar pill on `#step-card` (ADR-032, `DESIGN.md §5.11`): 10px WebKit channel, transparent track, thumb tinted to `--color-border` via `background-clip: padding-box` + a 3px transparent border; `scrollbar-width` left unset on the base rule (Chrome 121+ gotcha) and only set to `thin` inside an `@supports (-moz-appearance: none)` guard for Firefox.
- Added: `index.html` — a `.wizard-main { flex-direction: column }` override inside the existing `@media (max-width: 767px)` block. The row layout alone left no room for both the card and the rail at the existing 300px mobile wizard width (card collapsed to 47px against ~116px of minimum content); this restores the original stacked order (card above rail), the same fix already shipped in `graphics_module_1_topic_1_foundations`.
- Verified: interactively against the live shipped module (not a replica, per ADR-019) — clean boot, all 5 steps via Next/Back/rail-jump, 16 rapid quadrant switches, HP/VP slider two-way sync + clamp, fold/unfold gating, and `simAPI.reset()` all correct with zero console errors; CSS2D label count matched each step's view flags with no leftovers from prior steps. Could not reproduce the literal 50-rebuild live-GL-memory-flat measurement this session — this automated tab does not tick `requestAnimationFrame` (Chrome throttles rAF in backgrounded/inactive tabs), so `renderer.info` never advances; the disposal contract in `main.js` itself is untouched by this DOM-only change.

## 2026-07-03 (Term popovers + parameter dock — interactive UI complete)
- Added: `src/terms.js` — the glossary popover leaf: the Module-1 family singleton `#term-pop` (accent-wash, fixed-position, flip-above, Escape dismiss, `aria-describedby`) filled from the TERMS glossary, with listeners delegated from `#wizard` so the stepper's re-rendered bodies need no re-wiring; placement behaviour ported from the master `Module2/src/terms.js`.
- Added: `src/uiManager.js` — the parameter dock leaf (`initUIManager(simController)` → `{ sync, dispose }`): Distance-from-HP / Distance-from-VP slider ↔ numeric-field two-way sync (clamp on commit, invalid entry reverts to the last valid value and narrates the keep) and the Quadrant select, all routed through `simController.commit()`/`setQuadrant()`.
- Added: `index.html` — `#param-dock` markup + Module 2's token-only `.field` slider/select CSS, the `#term-pop` element + CSS, and the `--z-term` z-ladder token.
- Changed: `src/main.js` — inits both leaves; `commit()`/`applyView()` now re-sync the dock and the wizard chrome after every rebuild, so the dock's select and step 2's room buttons can never disagree, and dock controls grey out on steps whose view hides the point (step 1) or pins the quadrant (steps 3–5).
- Changed: `src/stepper.js` — dropped the interim native `title`/`aria-label` glossary wiring (it would double up with the real popovers).
- Verified: headless CDP per ADR-019 (shipped module, cache disabled) — 21/21 green: popover hover/focus/Escape paths + delegation across a step re-render, dock two-way sync/clamp/revert, quadrant cross-sync both directions, step-3 lock, and label DOM + live GL resources exactly flat across 50 slider-driven rebuilds and 50 resets, zero console errors.

## 2026-07-03 (CSS2D label layer — topic complete)
- Added: `src/labelLayer.js` — the CSS2D label leaf: HP/VP/XY plane callouts and boxed P/p/p′ chips (legacy `Module1/quadrants.js` anchors, so the topic reads identically to the lessons it replaces), the I–IV quadrant numerals with P's room highlighted ink+700 vs bench-grey+400 (Two-Cue), and visibility control of the BIS first-angle badge behind `showSymbol`; `clear()`/`dispose()` physically remove every label's DOM node (RULES.md §3.5).
- Added: `index.html` — `.lbl`/`.lbl--chip` label CSS and the `.fa-symbol` badge styling + `#fa-symbol` markup, all ported from Module 1's `shell.css`/`chrome.js` on the shared tokens so the family reads as one product.
- Changed: `src/main.js` — mounts a `CSS2DRenderer` overlay, builds/generates the label layer inside `rebuild()`, disposes it in the disposal contract, hinges its HP-riding labels (HP, p) from the same `easeFold` tween as the geometry leaves, syncs it on resize, and re-renders once fonts load (RULES.md §3.26).
- Verified: headless CDP per ADR-019 (shipped module, cache disabled) — labels correct on all 5 steps, active numeral follows the quadrant walk, p/HP labels ride the fold while p′ holds still, badge reveals on step 5, and label DOM nodes + live GL resources exactly flat across 50 resets with zero console errors.

## 2026-07-02 (3D content: planes, point, cinematic fold)
- Added: `src/hvPlanes.js` — the HP/VP plane pair leaf (teal floor / amber wall, faint fills + calm plane-hued grids + fat-line borders, VP dashed per the Two-Cue Rule) with the HP inside a pivot group hinged on the X fold line for the rabatment.
- Added: `src/point.js` — the point rig leaf: P as an ink sphere with paper halo, top/front views p and p′ as thick filled dots (ADR-016), dashed fat-line projectors (`LineSegments2`/`LineMaterial` + `computeLineDistances`), with the HP-riding pieces in their own hinge and an in-place-updated P→p projector.
- Changed: `src/main.js` — `rebuild()` now builds the leaves from the view flags with a leaf-owned disposal contract; `fold()/unfold()` tween the hinge 0↔90° over 1600 ms on `easeFold` (snap under reduced motion) instead of snapping; the render loop ticks anim.js so `simAPI.pause()` freezes a mid-swing fold; data-space → world remap (`worldPosition`) matches the legacy Module-1 draw3D convention.
- Changed: `src/anim.js` — replaced the drifted Foundations-derived copy with a byte-identical copy of `Module2/src/anim.js` (RULES.md §7.1; the old copy was missing `easeDraw`/`easeDissolve`).
- Verified: headless CDP per ADR-019 (shipped module, swiftshader) — clean boot, zero console errors, live GL buffers/textures/programs exactly flat across 50 rebuilds (2,550 created / 2,550 deleted), fold gate + unfold + reset correct, screenshots eyeballed for all 5 steps.

## 2026-07-02 (Guided stepper)
- Added: `src/spatialSteps.js` — pure data for the 5-step "Room & Shadow" sequence (Reference Planes → Four Quadrants → First-Angle Setup → The Fold → The Standard), with the TERMS glossary merged verbatim from the legacy `Module1/src/quadrantSteps.js` + `firstangleSteps.js` (10 terms; the 5 shared keys verified identical before merging) and the DEFAULT_VIEW flag vocabulary the geometry leaves will consume.
- Added: `src/stepper.js` — Module-2-pattern guided-stepper controller (`initStepper(simController)`): renders rail/card from the step data, progressive-disclosure panels, gates Next behind the step-4 fold, and pushes each step's view flags to the orchestrator via `applyView()`; term buttons get their glossary definition as native title/aria-label until a popover leaf lands.
- Changed: `index.html` — replaced the static placeholder card with the real wizard chrome (5-item rail, step card with eyebrow/title/lead/body/hint, quadrant picker panel, fold/unfold panel, Back/Next nav) plus its token-only CSS, so the stepper has DOM to drive.
- Changed: `src/main.js` — imports the data + wizard layers and exposes the injected `simController` contract (`getData`/`getView`/`isFolded`/`commit`/`setQuadrant`/`applyView`/`fold`/`unfold`/`announce`); `simAPI.reset()` now also restores data/view/fold state and re-homes the wizard to Step 1 through the single reset path.
- Verified: headless CDP check per ADR-019 (Node built-ins, cache disabled, shipped module) — clean boot, zero console errors, all 5 steps render, fold gate + reset behave; geometry stubs pending.

## 2026-07-02 (Data layer + registration)
- Added: `src/spatialData.js` — the pure data layer (point distances from HP/VP, quadrant enum + sign table, `resolvePosition()`/`quadrantAt()` resolvers), adapted from `Module1/src/pointData.js` with zero dependency on the retired `engine.js`, so `rebuild()` has a single bag of numbers to consume when content lands.
- Changed: registered this folder in root `ARCHITECTURE.md` §2 (SCAFFOLDED, NOT BUILT) and flipped the matching CLAUDE.md status rows, so the codebase map no longer omits the topic (MODULE-STARTER.md §3.10).

## 2026-07-02 (Scaffold)
- Added: Platform skeleton for the "Spatial Framework" topic (Quadrants + First-angle combined), built on Module 2's orchestrator pattern per ADR-033 — import map, design tokens, `meta.json`, `window.simAPI` contract, and an empty orbitable Three.js scene. No lesson content (HP/VP planes, point, fold, guided steps) yet.
