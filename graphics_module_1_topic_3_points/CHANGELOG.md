# Changelog — Projection of Points

## 2026-08-08
- Fixed: the Step 5 "Animate Unfolding"/"Fold back to 3D" rabatment could play far shorter than its designed 1600 ms if triggered while the opposite-direction fold was still mid-swing (e.g. unfold, then re-fold before the reverse settled) — `driveFold()`'s duration is `FOLD_MS * (remaining arc / FOLD_ANGLE)`, so a swing caught 300 ms into its reverse left `foldAngle` only slightly off the folded rest position, computing an arc of ~2% and a ~35 ms duration — a near-instant snap that read as a broken/too-fast fold. Confirmed via live foreground-browser timing (console-instrumented `performance.now()` around the tween) that a fresh reset+fold plays a consistent ~1580–1590 ms across all 4 quadrants, and that the interrupted-tween repro reliably reproduced the ~35 ms snap pre-fix. `driveFold(toAngle, { snap })` now lands `foldAngle` (and every leaf riding it) at the clean rest state opposite `toAngle` before computing arc when `snap: true`, guaranteeing the full `FOLD_MS` on every deliberately-watched fold. Scoped to the two explicit `simController.fold()`/`unfold()` entry points only (covers the wizard buttons, the Front quick-view chip's re-click reversal, and `enterWorkbench()`'s return-to-3D) — `applyView()`'s own silent step-navigation reverse-sync keeps the unsnapped proportional resume, since that path isn't a watched animation and forcing a full 1600 ms there would stall ordinary back/forward step navigation. Not quadrant-specific: the bug is triggered by fold-interrupt timing, not by which quadrant is selected. (`main.js`.)

## 2026-07-31
- Added: "Finish lesson" button (Module 2 Finish-button pilot rollout) — `#btn-finish` takes over the footer's primary slot at the terminal Step 5 "Unfold" exactly when `#btn-next` vacates it, enabled once folded. Click posts `sim:complete` and announces "Lesson marked complete." (`main.js`, `src/stepper.js`, `index.html`.)
- Changed: `sim:complete` (`markComplete()`) drops its one-shot `window.__simComplete` latch — fires on every "Finish lesson" click now, replacing the old auto-fire on first fold (the fold's own toast is unchanged, still one-shot). (`main.js`, `src/stepper.js`.)
- Changed: `#btn-complete-next` demoted off "Complete & next problem"/"Pick a problem" wording to a single "Try another problem" label — Finish lesson now owns the completion signal, so this stays the repeatable practice-loop action only, same label in both problem and free-play modes. (`src/stepper.js`, `index.html`.)

## 2026-07-28
- Added: a new `markComplete()` posts `{ type: 'sim:complete' }` to `window.parent` once, fired on the first fold alongside the existing "Projection of Points completed!" toast — the host's second sanctioned signal, for a "next topic / stay" overlay (ADR-078 addendum). (`main.js`, `src/stepper.js`.)

## 2026-07-27
- Changed: the Problem Library overlay's title now centers in its header row (was hard-left) — a 44px spacer counterweights the close button so it stays corner-anchored (ADR-082). (`index.html`.)

## 2026-07-25
- Fixed: the Compare split's floating "compact" card (its own title bar + expand/close buttons) could get stuck stranded at full window width after the viewport narrowed below 768px and then widened back — a one-way `matchMedia` listener demoted the split but never restored it. Removed the compact card entirely: Compare now has exactly one shape (the docked 50/50 split) at every viewport width, and below 768px the same grid restacks to a single column via CSS instead of switching UI (ADR-080, platform-wide). The wizard-toggle chevron's split-exit branch now calls `compare.hide()` instead of demoting to compact. (`main.js`, `index.html`.)

## 2026-07-24
- Added: `markBooted()` now posts `{ type: 'sim:ready' }` to `window.parent` once, after `document.fonts.ready` resolves — the host loading screen's boot-ready signal (ADR-078, narrows ADR-002). (`main.js`.)

## 2026-07-20
- Changed: `--color-vp-line` darkened `#bc5d1e → #b25718` (platform-wide AA promotion, ~4.92:1 on paper) — `index.html` `:root`.
- Changed: `.step-body p` gained `color: var(--color-ink-secondary)` so the multi-paragraph step prose reads grey like the rest of the step card instead of near-black `--color-ink` — part of a platform-wide step-card typography pass matching Module 2 (ADR-073).

## 2026-07-16
- Added: drag-to-pan and scroll-wheel zoom on the 2D Compare sheet (Module 2 ADR-054/055 parity) — `comparePanX/Y` + `compareZoom` (clamped 0.4–5×) post-multiply over the existing `REF_SPAN`-locked scale via a new `project()` choke point in `drawCompare()`, so the fixed-scale sheet math is untouched. Dblclick recenters and un-zooms. No live-position anchor was added (unlike Module 2's off-centre side-view block, the Points sheet is already `REF_SPAN`-symmetric — a live anchor there would jump the drawing across the XY line, so dead-space balancing is left to user pan by design). Verified via CDP: wheel clamps exactly at 0.4/5×, drag delta matches `comparePanX/Y` exactly, dblclick resets to 0/0/1. (`main.js`.)

## 2026-07-15
- Fixed: the collapsed `#rail-toggle` ("Show") pill sat flush with `#sim-viewport`'s bottom-left corner, overlapping its `--radius-md` curve instead of clearing it — the 2026-07-14 note below verified its 16px offset from the *window* edge, but the card itself is already inset 16px from the window by the grid padding, so button-edge and card-edge were coincident (0px card inset). Now anchors at `left/bottom: var(--space-6)` (32px = 16px gutter + 16px inset), landing exactly 16px inside the card corner. Verified via headless-Chrome CDP: `getBoundingClientRect()` on both elements gives `btn.left - card.left === 16` and `card.bottom - btn.bottom === 16`. (`index.html`.)
- Fixed: `exitWorkbench()` now also calls `syncRailToggleState(false)` right after clearing `rail-collapsed`, pairing every rail-collapsed mutation with a button-facet sync (defensive — the button is `display:none` outside the split) — symmetric with `enterWorkbench()`'s existing forced-open sync. Verified the underlying forced-open fix via headless CDP: isolated the exact regression (button stuck on "Show" after collapse → exit → re-enter) by re-running the same click sequence against a copy with only `enterWorkbench()`'s sync call removed, confirming that call is what prevents the desync. (`main.js`.)

## 2026-07-14
- Changed: Compare-split trials the "rounded cards on gray panel" layout ported from topic 4 — the 3D viewport, 2D drawing card, and driver rail now float as three distinct white rounded cards (`border` + `border-radius: var(--radius-md)`) on a `var(--color-panel)` grid backdrop with `var(--space-4)` gap/padding, replacing the flush hairline-seam layout. Temporarily suspends ADR-021 (same suspension already trialed on topic 4); revertable by restoring the flush borders/seams this replaces. Verified live via headless Chrome/CDP: three non-overlapping rounded cards, gray backdrop confirmed via computed styles. (`index.html`.)
- Changed: the rounded-cards Compare-split layout above is officially adopted — ADR-021 and ADR-037 amended in `DECISIONS.md`, no longer a "trial." (`DECISIONS.md`.)
- Added: a `#rail-toggle` button hides/reveals the `#workbench-rail` driver rail while the split is open, for a full-screen 3D/2D read. Collapsing it drops the rail's grid row so the viewport and 2D drawing card reclaim the freed height, then re-measures via `remeasureAfterReflow()` so both canvases resize crisply. The rail always resets to shown on re-entering the split; the wizard chevron (`#wizard-toggle`) is untouched. (`index.html`, `main.js`.)
- Changed: `#rail-toggle` gains an explicit visible "Hide"/"Show" text label (was icon-only) and relocates from a fixed bottom-right chevron to the workbench rail's top-left — it shares `grid-area:rail` with `#workbench-rail` while the split is open (floats over the rail's own top-left inner padding), then re-anchors `position:fixed` to the screen's bottom-left once collapsed, since the "rail" grid area no longer exists at that point. It was already a direct `<body>` child (not nested in `#workbench-rail`), so it already survived collapse — this pass only changes its position/label, not its DOM parent. Verified headless via CDP (Edge, `--headless=new`): open-state sits within 60px of the rail's top-left corner, collapsed-state sits fixed bottom-left and stays on-screen (`offsetParent` is `null` for any `position:fixed` element per the CSSOM View spec regardless of visibility, so survival was checked via computed `display`/`visibility` + a non-zero rect instead), label/aria-label swap Hide↔Show correctly, and both panes measurably grow/shrink with the rail row. (`index.html`, `main.js`.)
- Fixed: `.wizard-toggle`'s top offset was inheriting the 68px LMS-scaler clearance meant for the top-LEFT `.vp-cluster`, leaving dead space above the top-right wizard chevron; now `top: var(--space-3)` (12px). Confirmed the collapsed `#rail-toggle`'s `left:0; bottom:0; margin: var(--space-4)` already renders 16px off both edges (no separate fix needed there). Verified headless via CDP: wizard-toggle rect top = 12px, collapsed rail-toggle rect left = 16px / bottom-gap ≈ 16px. (`index.html`.)
- Fixed: in the 2D Compare sheet, the distHP and distVP dimensions overlapped in Quadrants II/III — both are vertical dims stood off to the same `DIM_OFF_V`, and in those two quadrants they share a sign so both landed on the same line. `drawCompare()` now stands the larger of the two out further (`DIM_OFF_V + 26`px), or splits them left/right when equal, per BIS SP 46:2003 parallel dimensioning. Verified live (Q2, distHP=30/distVP=40): two distinct dimension lines with clean paper between them, confirmed by canvas pixel sampling. (`main.js`.)
- Fixed: the I–IV quadrant numerals (CSS2D labels) could bleed through the top-left quick-view cluster's transparent gaps — a CSS2D node is a real DOM element, so neither the depth buffer nor normal paint order can clip it against in-viewport chrome. `updateOcclusion()` now also hides a numeral whose projected screen position falls inside `.vp-cluster` / `#compare-chip` / `.wizard-toggle`, alongside its existing mesh-occlusion check. Verified with a synthetic camera against the live page's real chrome rects: a numeral projected into the cluster reports `hidden`, one projected clear of it stays visible. (`src/labelLayer.js`.)
- Changed: `#workbench-rail` in the Compare split gains its own stacking context (`position: relative; z-index: 20`) as defensive hardening, independent of the numeral fix above. (`index.html`.)
- Fixed: `#rail-toggle`'s label could desync from the rail's actual visibility — `enterWorkbench()` force-reopens the rail on every split entry but never touched the button, so collapsing the rail, shrinking to the compact card, then re-expanding left the button reading "Show"/collapsed while the rail was visibly shown. Centralized the button's 4 state facets (`aria-expanded`, `aria-label`, `title`, the `.rail-toggle__text` span) into one `syncRailToggleState()`, called from both the click handler and `enterWorkbench()`. (`main.js`.)
- Fixed: the 2D Compare sheet's shared vertical projector overshot both point marks by 10px on each end (a `± 10` pad in `drawCompare()`), against N.D. Bhatt/BIS drafting convention that a projector terminates exactly at the point it locates. Projector now runs exactly between the two view marks; the marks' paper halos already cover the endpoints, so the line still reads clean at each disc. (`main.js`.)

## 2026-07-10 (Compare-split viewport sharpness + left-only rail seam)
- Fixed: the 3D viewport rendered soft/mis-scaled in the 50/50 Compare split — entering the split flips the body from a flex row to a CSS grid, and the old resize ran inside a single `requestAnimationFrame` that fired before the grid reflow was laid out, so it measured the pre-split full width and pinned a stale, oversized canvas into the half pane. A new `remeasureAfterReflow` waits a second frame so the renderer is sized to the true pane; verified in-browser (canvas backing = `floor(cssPx × devicePixelRatio)`, razor-sharp in split). (`main.js`.)
- Fixed: the device-pixel ratio is now re-asserted on every resize (`renderer.setPixelRatio`), not just at boot — so browser zoom, a monitor move, or the split can no longer break the 1:1 backing-store mapping. (`main.js`.)
- Changed: the structural hairline under the split now spans only the LEFT (3D) half — it moved from the rail's full-width `border-top` onto `#sim-viewport`'s own `border-bottom`, so the right half (2D drawing → rail) reads as one seamless workbench surface. (`index.html`.)

## 2026-07-10 (Workbench rail polish — clutter, spacing, seam)
- Removed: the quadrant hint (`#quad-note`, "Above HP, in front of VP") is now hidden entirely in the Compare split — the pressed I–IV button already names the room and `#sim-status` narrates changes, so the note was redundant clutter that made the horizontal rail messy. Supersedes the earlier "beside the picker" reflow. (`index.html`.)
- Changed: the quadrant driver drops its bespoke 2-column grid (built only to seat the note) and falls back to the shared `.field` flex column at 248px, so the four I–IV buttons sit under their label exactly like a slider sits under its own — the rail now reads as one uniform instrument bench. (`index.html`.)
- Changed: rail breathing room — inter-field gap `--space-5`→`--space-6` (24→32px) and padding `--space-3 --space-4`→`--space-4 --space-5` (12/16→16/24px); each field's label→control gap `--space-1`→`--space-2` (4→8px). Cramped controls now have even, premium spacing. (`index.html`.)
- Verified: the 2D Compare card→rail seam is a single hairline (ADR-021) — docked card carries border-left only, no bottom border, no radii, no shadow; rail carries the lone top hairline. Confirmed via computed styles in-browser. (`index.html`.)

## 2026-07-10 (UI polish + Compare 2-step visibility fix)
- Fixed: opening Compare while the sheet was folded split the screen but left the 2D (S2) drawing blank until a second click. Root cause: `enterWorkbench` sets `compareOpen`/`workbenchOpen` then unfolds the sheet, and `driveFold` unconditionally closed the Compare card — tearing down the split it was building and skipping the paint. `driveFold` now closes Compare on a FORWARD fold only, so the S2 sheet paints on the first click. (`main.js`.)
- Fixed: the viewport rendered slightly soft — `setSize(…, false)` left the canvas CSS size to `width/height:100%` (a fractional container box) while the backing store used integer px, a sub-pixel upscale. New `pinCanvasSize` pins the display px to the integer logical size (+ `devicePixelRatio || 1` guard). (`main.js`.)
- Added: the fold swoop now lights the **Front** quick-view chip when it lands square-on the flat answer sheet, and re-clicking that lit chip physically REVERSES the fold back to 3D (resetting the fold button) instead of just gliding the camera off a still-flat sheet. (`main.js`.)
- Changed: the 'P' chip now hugs the point — offset `[0.40,0.58,0.40]` → `[0.30,0.44,0.30]` (still clear of p′). (`src/labelLayer.js`.)
- Changed: in the Compare split the quadrant hint ("Above HP, in front of VP") reflows to sit BESIDE the quadrant picker in the empty right-side rail space instead of stacking beneath it. (`index.html`.)

## 2026-07-10 (fold swoop — orthographic answer-sheet landing, ADR-036)
- Fixed: the step-5 fold now SWOOPS square-on to the orthographic answer sheet (ADR-036, overturning ADR-013's held-angle hold) — `driveFold` forward calls a new `swoopToAnswerSheet` (engage the ortho camera, glide front-on to the flattened sheet via `fitOrthoZoomForView('front', …)`, and morph perspective→ortho on the hinge's `easeFold` curve); reverse glides back to the learner's retained perspective orbit pose via `restorePerspective`. The held-angle movers `framePerspectiveToFlat` + `fitPerspectiveDistance` — and the now-unused `preFoldPose` and `FRAME_PADDING` — are deleted; the perspective camera no longer moves during the fold, so the reverse restore needs no stored pose. (`main.js`.)

## 2026-07-10 (2D drawing scale lock + Compare-split chrome cleanup)
- Fixed: the 2D Compare drawing auto-zoomed — its scale was `frameHalf / the point's own value`, pinning p / p′ to the frame edge so 10 mm shrank on screen as a slider grew (the sheet seemed to shrink around a fixed-size mark). Now a FIXED scale keyed to the 40 mm slider max, so 10 mm always reads as 10 mm and the mark travels outward as the value grows. (`main.js` `drawCompare`.)
- Fixed: the 3D **VP** plane callout overlapped the "Compare 2D drawing" chip — dropped its wall anchor (y 3.7 → 2.5) so it sits lower on the VP wall, clear of the top-left control cluster. (`src/labelLayer.js`.)
- Changed: in the 50/50 Compare split the redundant 2D-drawing head (`.compare-card__head`) and the wizard chevron (`#wizard-toggle`) are hidden — the still-visible, pressed Compare chip in the left pane is the split's single exit (mirrors the Lines workbench). (`index.html`.)
- Changed: the Quadrant selector lays out as a single horizontal row (I II III IV) in the workbench rail, matching the Lines rail — was a 2×2 grid. (`index.html`.)
- Verified: local server + Chrome — distVP 20→40 moves p exactly 2× further from XY (scale constant); VP label clears the chip; split hides head + chevron and exits via the chip; quadrant row is 1×4.

## 2026-07-10 (Compare-split workbench — true 50/50 parity with Lines, ADR-037)
- Added: expanding Compare now opens a **true 50/50 split workbench** — the step wizard collapses, the live 3D viewport takes the left pane, the 2D orthographic drawing takes the right, and the four point drivers (Quadrant, Distance from HP/VP/PP) re-parent into a docked `#workbench-rail` under both panes so you can tune all four at once and watch the 3D and 2D update together. Desktop opens straight into the split (`COMPARE_DEFAULT_SIZE`). Why: parity with the Lines workbench (ADR-021), which Points had been excluded from — the "solve & verify" altitude the floating card couldn't give. (`main.js` `enterWorkbench`/`exitWorkbench`/`applyCompareSize`, `index.html` `body.compare-split` CSS grid + rail, `src/stepper.js` `refresh`.)
- Added: the drawing card head's shrink button drops the split back to the ADR-012 floating compact card; the wizard chevron and Compare-close both exit the split; on exit `stepper.refresh()` restores the wizard's per-step progressive disclosure of the borrowed drivers. (`main.js`.)
- Changed: **not** implemented via engine config keys — the task's `initSim({ compareSplit, compareDefaultSize, workbenchControls })` keys don't exist in this topic (Points was migrated off the shared `engine.js`, ADR-033); the split is built in the topic's own architecture instead. The single-WebGL-canvas invariant is preserved: the right pane reuses the existing live-repainted 2D `<canvas>`, so no second GL context is created. See ADR-037; ADR-021's "Points omits `cfg.workbenchControls`" clause struck.
- Verified: local server + Chrome — split opens 50/50 (3D left correct-aspect, 2D right), changing Quadrant I→III in the rail updates BOTH panes together (point jumps to Q3, drawing flips p above / p′ below), shrink restores full-width 3D + only the current step's control, expand round-trips; zero console errors across enter/edit/exit/re-enter.

## 2026-07-09 (Points UI polish — quiet scrollbar, pinned nav footer, 40 mm cap, smaller P)
- Changed: the `#step-card` and `.problem-library__body` scrollbar pills retinted accent-blue → `--color-border` (ADR-032 Quiet Chrome — matches the step connector lines); WebKit thumb + Firefox `scrollbar-color`. (`index.html`.)
- Changed: the step card adopts the Foundations pinned-footer layout — the card itself never scrolls (`overflow: hidden`); a new inner `.card__scroll` region scrolls the header + body, so the Back/Next/Reset footer stays permanently visible at the card bottom. stepper.js scroll-reset retargeted to the inner region. (`index.html`, `src/stepper.js`.)
- Changed: distance sliders capped at 40 mm — `distHP`/`distVP` 0–40, `distRP` −40…40, and the typed-field ceiling now matches (was 100 slider / 200 typed); all textbook problems stay ≤ 40 mm, so nothing is unreachable. (`index.html`, `src/uiManager.js`, stale range comments in `main.js`/`src/pointProblems.js` synced.)
- Changed: the 3D point sphere `P_RADIUS` reduced 0.16 → 0.12 world units so P reads precise and proportionate. (`src/point.js`.)

## 2026-07-09 (Phase 1 Final — white card, ortho orbit lock + cue, numeral occlusion)
- Fixed: `#step-card` retinted panel-grey → pure white `--color-paper`, so the wizard content area reads white on the grey panel shell. (`index.html`.)
- Added: ortho orbit lock — `orthoControls.enableRotate = false` (a quick-view / flat pan is a square-on 2D read; pan + zoom stay live), with a `qv-lock-cue` shake+focus-ring nudge on the latched chip (or the chip group) when a left-drag is attempted on the locked ortho camera. (`main.js`, `index.html`.)
- Fixed: the I–IV quadrant numerals no longer read through the HP/VP sheets — a per-frame `labelLayer.updateOcclusion()` raycasts camera → numeral against the sheet FILL meshes (Line2 grids excluded: they extend Mesh but their raycast needs `Raycaster.camera`) and hides any occluded numeral; runs after `renderer.render` (fresh matrices), before the CSS2D re-projection. (`src/labelLayer.js`, `main.js`.)

## 2026-07-09 (Quadrant numerals pushed to grid corners)
- Fixed: the I–IV room numerals sat mid-space at `[0.25, ±2.7, ±2.7]`, overlapping/clashing with P, p, p′ and the projectors at common slider values — moved to each room's far outer corner (`[4.1, ±4.1, ±4.1]`, just inside the 4.5-unit sheet edges, right end of the fold line, clear of the HP/VP/X callouts on the left). (`src/labelLayer.js` `QUAD_ANCHORS`.)

## 2026-07-08 (Phase 6 — UI/UX polish: quadrant grid, negative PP, Compare mirrors lateral)
- Changed: the Step-1 Quadrant control is now a 2×2 button grid (I/II · III/IV) with a live definition note beneath it, replacing the legacy `<select>` — the selection pattern proven in the Topic 2 Spatial Framework (`data-quadrant` buttons + a controller-stamped `aria-pressed`), laid out as a grid here; the note reads the exact standard definition of the chosen room ("Above HP, In front of VP", etc.). Why: a spatial 2×2 reads faster than a dropdown and keeps the four rooms visible at once. (`index.html`, `src/uiManager.js`; `main.js` `firstControl` now also matches `.quad-btn`.)
- Added: the Distance-from-PP slider now spans BOTH sides of the profile plane (−100…100 mm; typed field ±200 mm), so a point can be walked left and right of PP along the fold line — negative = left of PP. `pointData.resolvePosition` already passed `distRP` through with its sign; only the slider/field bounds and clamp needed widening. Why: lets students see lateral movement in both directions. (`index.html`, `src/uiManager.js`, `src/pointData.js` docs.)
- Fixed: the Compare card's 2D drawing ignored the lateral (PP) coordinate — the projector was always centred, so moving the PP slider changed nothing on the sheet. It now DRAWS the lateral position: the shared projector + both view marks slide left/right of a centre datum along the XY line (with a datum tick + a lateral dimension when off-plane), so the drawing truly mirrors the 3D state and the negative-PP walk is visible. Why: "perfectly mirrors the 3D state" was failing for distRP. (`main.js` `drawCompare()`.)
- Fixed: the Compare view marks were plain colour discs — now a paper HALO under the colour disc, matching ADR-016's "thick filled dot = paper halo + colour disc" for p / p′. (`main.js` `drawCompare()`.)
- Verified: headless Chrome + SwiftShader — 23/23 checks GREEN; the 2×2 grid renders `display:grid` with 4 buttons and the exact per-quadrant note, the `<select>` is gone, the distRP slider clamps −100…100 and accepts −80, the Compare canvas paints and repaints live on distRP drag (both signs), and live GL resources (Buffers 53, Textures 4, Programs 4, VAOs 19) stay flat across 50 resets AND 30 quad+distRP churns, with zero console errors.

## 2026-07-08 (Dual-camera ortho quick-views — enforce RULES.md §5.18)
- Added: a second `OrthographicCamera` + its own OrbitControls alongside the perspective camera, so the Top/Front/Side quick-views are now TRUE orthographic reads (no foreshortening) fitted to the plane pair + point (`main.js` buildScene, `engageOrtho`/`tweenCamFull`/`contentBoxWorld`).
- Added: the `projectionMorphK` dual-camera matrix morph (RULES.md §5.18) — clicking a chip element-wise lerps the ortho projection matrix from the perspective one and back on re-click, so the perspective↔ortho hand-off gains/loses depth smoothly instead of hard-cutting (`applyProjectionMorph`, stamped in the render loop; `restorePerspective` glides back to free orbit).
- Changed: every camera-move seam (quadrant flight, fold, reset, drag) now hands the live camera back to perspective before it drives, so the two cameras never fight; the render loop draws/updates `activeCamera`/`activeControls`.
- Fixed: `restorePerspective` referenced `easeStandard` which was never imported — added it to the `anim.js` import (headless verification caught the runtime ReferenceError that `node --check` could not).
- Removed: rogue `ADR-035` (DECISIONS.md) and `§5.19` (RULES.md) that had documented the single-perspective-camera deviation — reversed per user directive; §5.18 governs and this topic is now an `orthoViews`-style dual-camera lesson.
- Verified: headless Chrome + SwiftShader — 15/15 checks GREEN; live GL resources (Buffers 53, Textures 4, Programs 4, VAOs 19) and CSS2D labels (15) stay flat across 50 resets, 12 quick-view engage/return cycles, and a reset-from-ortho; the morph engages/returns and clears its chip; zero console errors.

## 2026-07-07 (Phase 5 — final polish: line conventions, quick-views, leak cert)
- Fixed: the Compare card's 2D orthographic projector was drawn dashed — now a SOLID Type-B continuous thin line, matching ADR-016 (in the 2D drawing the HP/VP read is carried by position, so only the 3D pictorial projectors stay dashed); `main.js` `drawCompare()`.
- Added: the ortho quick-view camera chips (Top/Front/Side) are now wired — `index.html` un-hides the `.quick-views` cluster and `main.js` gains `setupQuickViews()` + `simController.setView()`, flying the single perspective camera square-on to each reference plane via the existing eased `flyCamera()`; re-clicking the lit chip returns to orbit.
- Changed: a lit quick-view chip now clears whenever another motion takes the camera — a user orbit drag, a quadrant flight, the fold, or reset — so no chip stays pressed for a pose the camera has left (`clearQuickView()`).
- Verified: headless Chrome + SwiftShader certification GREEN — live GL resources (Buffers 53, Textures 4, Programs 4, VAOs 19) and CSS2D `.lbl` label nodes (9) stay perfectly flat across 50 rapid `simAPI.reset()` cycles and 50 rapid slider commits, with zero console errors; a second headless pass confirms the quick-view chips fly the camera and toggle state through the shipped wiring.

## 2026-07-07 (Phase 4 — Problem Library + textbook problem sets)
- Added: `src/pointProblems.js` — the K.C. John / N.D. Bhatt textbook point problems, ported verbatim from legacy `Module1`, in three tiers (first-quadrant basics → four quadrants → on-plane); on-plane cases keep their OR-array targets so a degenerate point (zero distance) is graded correct in either coincident quadrant (ADR-015).
- Changed: `src/problemLibrary.js` replaced the leftover solids controller with the legacy generic, data-driven one — a ±0.5 mm tolerant OR-array self-check that never auto-fills; loading a problem resets to defaults and routes to Step 1 so the learner dials the answer by hand. Adapted to the Module 2 orchestrator: the "complete & next" affordance moved out of the library into the stepper.
- Removed: Stale `src/problems.js` (solids data importing the deleted `shapeData.js`) — orphaned once the points library landed.
- Changed: `main.js` imports + initialises the library, adds the `notifyStateChange()` seam fired at the end of every `rebuild()` (drives the self-check), and extends `simController` with the library facade (`state`/`hasWork`/`isBusy`/`goStep`/`onStateChange`/`isProblemActive`/`completeAndNext`).
- Changed: `index.html` un-hides the "Practice problems" entry and adds the terminal-step `#btn-complete-next`; `src/stepper.js` reveals it once the drawing is folded and routes it through `completeAndNext()` to reopen the library.
- Verified: headless WebGL check green — on-plane problem `pt-m` loads and lights the green self-check at `distHP=0, distVP=30` for BOTH Q1 and Q4 (OR-array) while correctly rejecting Q2; the fold → "Complete & next problem" → reopen-library flow works end-to-end; zero console errors.

## 2026-07-07 (Phase 3 fixes — ADR compliance)
- Fixed: Slider bounds were 1–40 mm, breaking the Phase-4 K.C. John textbook problems that need points AT a reference plane — sliders now run 0–100 mm with typed entry accepted up to 200 mm (`uiManager.js` + `index.html`).
- Fixed: ADR-012 regression — the mandatory Compare card is back: chip + card chrome ported from `Module1/src/chrome.js`, the `compare` state machine from `Module1/src/engine.js`, the Unfold step's "Open Compare…" sentence restored; the card's 2D sheet draws on a plain 2D canvas, live-repainted every rebuild (ADR-034 records why not a second WebGL stack).
- Fixed: ADR-013 regression — the fold no longer locks the camera: `driveFold()` now runs the held-angle hold (`framePerspectiveToFlat` + `fitPerspectiveDistance` ported from the legacy engine), dollying/panning along the learner's view direction to frame the flattening sheet, storing and restoring the pre-fold pose.
- Verified: headless WebGL check green — boot, 0 mm typed commits, chip gate on step 3, card open/draw/expand/close, fold auto-closes the card, camera moves during the fold and restores exactly on unfold, labels flat across 50 rapid commits + 6 compare cycles, reset clean.

## 2026-07-07 (Phase 3 — UI shell, guided sequence, labels)
- Added: `src/pointSteps.js` — the legacy 5-step Points sequence (Quadrants → HP → VP → PP → Unfold) + term glossary, copy verbatim from `Module1/src/steps.js` (the legacy lesson is 5 steps, not 6; one "Compare" sentence trimmed since this build has no Compare card).
- Added: `src/labelLayer.js` ported from the Spatial Framework sibling — HP/VP/X/Y callouts, I–IV numerals, P/p/p′ chips, plus a new `P(x, y, z)` signed-mm coordinate read-out and a `setSymbol()` hook for the fold-driven BIS badge.
- Changed: `src/stepper.js`, `src/uiManager.js`, `src/onboarding.js`, `src/terms.js` rewritten for PointData — data-driven wizard with one control per step, mm sliders + quadrant dropdown (ADR-018), legacy Points spotlights, delegated `#term-pop` glossary; solid-specific logic (face inclination, add-solid empty state) removed.
- Changed: `src/point.js` splits `showProjections` into independent `showHP`/`showVP` so the top and front views reveal one shadow at a time, matching the legacy progressive disclosure.
- Changed: `main.js` wires the wizard, dock, onboarding, CSS2D label overlay, and the 1600 ms rabatment fold (camera never moves — legacy Points behaviour); `index.html` swaps the solids panels for the 5-step rail + 4 Points controls and hides the not-yet-wired Problem Library entry and quick-view cluster.
- Verified: headless WebGL boot + full 5-step walk green — fold/unfold, back-navigation auto-unfold, reset, label-leak check (flat across 40 rebuilds), zero console errors.

## 2026-07-06 (Phase 2 — pipeline rewire)
- Added: `src/pointData.js` data layer ported from legacy `Module1` — `distHP`/`distVP`/`distRP` + quadrant with sign resolution (`resolvePosition`).
- Added: `src/hvPlanes.js` and `src/point.js` ported verbatim from the Spatial Framework sibling — the polished HP/VP plane pair and the point rig (P, p, p′, projectors).
- Removed: Remaining solid-specific leaves `meshAnalyzer.js`, `projectionDrawer.js`, `vertexLabeler.js`, `iShape.js` — a dimensionless point has no edges to weld, faces to project, or vertices to label.
- Changed: `main.js` rewritten as the points orchestrator — `rebuild()` now consumes `PointData` (disposal contract → planes → point rig), with the Topic 2 quadrant camera-pose table + slerp orbital `flyCamera()` replacing the solids framing; boots green in the headless WebGL check.
- Changed: Solid-shaped UI leaves (`stepper.js`, `uiManager.js`, `problemLibrary.js`, `onboarding.js`) are intentionally not imported yet — `problems.js` still imports the deleted `shapeData.js` — their PointData rewire is the next phase.

## 2026-07-06
- Added: New topic `graphics_module_1_topic_3_points`, cut from the Module 2 master skeleton per ADR-033 (Module 1 migrates onto the orchestrator pattern).
- Removed: Master git/scratch metadata (`.git/`, `.claude/`, `.impeccable/`) and the copied `CHANGELOG.md` so the topic starts clean.
- Removed: Inherited local `DESIGN.md` — the topic consumes the single root `../DESIGN.md` (ADR-028), matching the Spatial Framework pattern.
- Removed: Solids-specific `src/` files (`cube.js`, `cone.js`, `cylinder.js`, `genericPrism.js`, `genericPyramid.js`, `genericSolid.js`) and `shapeData.js`, since this topic teaches 3D points, not solids; `pointData.js` arrives in the next phase.
- Changed: `meta.json` identity — title "Projection of Points", beginner difficulty, quadrant-focused description and tags.
- Changed: `index.html` `<title>` and `<meta description>` to match `meta.json` (ADR-026); import map and design tokens left untouched.
- Changed: `CLAUDE.md` H1 and opening description rewritten for this topic; root-doc pointer block retained.
